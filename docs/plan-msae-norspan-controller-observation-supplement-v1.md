# Prospective observation-scope and synthetic timing supplement v1

2026-09-16. Guarded source-free successor only; all earlier reviews retained.

Actual bounded local Git status on a fresh synthetic repository invoked a configured
clean helper (outside marker only). Disabling hooks/fsmonitor is NOT helper exclusion
or network isolation. Recovery must replace its ambiguous network_operations=0 with
source_network_operations=0 and explicitly label counts controller-direct-calls only,
not status-helper side effects. Report helper effects/protected-content exclusion and
network isolation as UNVERIFIED, not zero or established. Local status attempts and
completions remain separately counted; unchanged actual evidence validators remain.
Never authorize real status, source work, scoring or training from these observations.
Real trusted-status containment is still a mandatory unqualified launch boundary.

A synthetic FIFO test's single3s timeout includes cold Python imports and NFS cache
creation as well as the actual reader. If completed retained independent tracebacks
confirm bootstrap-time failures, separate a bounded30s bootstrap READY handshake from
an UNCHANGED3s reader-operation deadline. Disable child bytecode writes; preserve all
failed evidence, kill/reap the owned child on any error, never retry or treat timeout
as a rejection pass. READINESS can only be emitted after imports finish, immediately
before the same actual reader. Both phases must complete and reader must reject FIFO.
This is harness qualification, not relaxed production or reader limits.

Normalize only four public ordinary admission modes to the existing declared0644:
TODO.md and new authority/controller/full-recovery tests; preserve original TODO bytes.
Fixtures must assert finite real PUBLIC input modes before copying, not normalize an
invalid actual candidate into a falsely valid copied release. Synthetic whole-review
fixture remains conspicuously test-only. Freeze new exact bytes/modes and independently
requalify; old49 candidate and every BLOCK/REVISE stay immutable. Add the finite legacy
ordinary signer-input subjects missing from repair-only49freeze; no protected inputs.
