# Prospective NORSPAN marker-fault qualification wave1 — 2026-09-17

## Goal and non-goals
Expand executable, firing qualification at the actual permanent-pair publication,
paired registration/outside catalog and interruption boundaries of all eleven
previously declared marker names. Close only explicitly enumerated primitive/
publication-adapter cells, not the whole marker group, original80 audit rows or
scientific/producer control-flow. Current exact65 remains immutable engineering
history and production guard remains first/unconditional.

Owner approved continuing work on2026-09-17. This is not a waiver of technical
qualification, source/readiness/scoring/once-only gates or an enrollment of a final
exact production approval receipt. Owner is the chosen trust root; no external
signer/key/admin required. Existing outside requests stay PENDING.

## Grounding and constraints
DES-0203/0221 require Jumbo; proposed DES-0231 does not prove server/hard bounds.
Design queries for NORSPAN/fault publication returned no matches; directly inspected
these entries. bin/query refs publish_json with the exact public scope refuses the
undeclared nested repository, so finite public reads establish wiring:
prepare:publish_json -> publish_bytes -> pair:create_pair/create_owned_pair ->
ControlSession.register -> CatalogStore.checkpoint/_write_snapshot.
Controller.execute poisons an invocation after any exception; existing eleven-marker
actual full synthetic checkpoint failures are separately retained in exact65.

All output/cache/basetemp/evidence must be newly allocated below
/jumbo/lisp/f004ndc/tmp/lisplab1. No real history/status/authority/acquisition/source/
raw/private/payload/model/scoring/GPU/tmux/K2/branch/G3/C2. Never read protected keys,
Atlas finals, actual V8/V9/V10/NORSPAN evidence. No initiated retry, destructive
cleanup, overwrite/discovery/adoption or automatic commit/push. Original protocol/
config/TODO/V10 bytes and all previous reviews/checkpoints stay unchanged. NEVER
collect tests/test_prepare_msae_independent_norspan_v1.py (public source-only).

## Approach and simpler alternative
First add ONE explicit source-free test file. Minimal fresh Jumbo synthetic roots
exercise actual publication adapter, real pair primitive and real outside catalog;
marker bodies are explicitly synthetic canonical test JSON, NOT scientifically typed
state. Do not mock publication/custody success. Existing full typed-controller cells
continue to supply distinct control-flow evidence; direct-adapter cells alone cannot
close it. Faults must fire exactly once at a named target, independently of monitor,
helper/catalog or earlier pair activity, and demonstrate original/foreign retention,
no outside pin advance/adoption, all original descriptor release and no false success.

Prefer test-only qualification if current code meets the contract. Do not invent a
production rewrite to satisfy a test count. If a genuine code defect is discovered,
retain the original failing candidate/transcript and STOP that repair for a separately
reviewed bounded test-first supplement. No unreviewed production edits in this wave.
An enormous whole cross-product is rejected as a single milestone: bounded cells
are useful only with explicit remaining obligations and no whole acceptance claim.

## Milestones and acceptance
- W0: freeze exact65 before-image, record read-only owner/cgroup metadata and obtain
  independent prospective plan SHIP. No cgroup directory/limits/membership writes.
- W1: all11 markers x first-stage-open/write/chmod/first-file-fsync/first-parent-fsync/
  final-link/second-file-fsync/second-parent-fsync/first-complete-validation/final-byte-
  fence/owned-file-close x OSError/KeyboardInterrupt, injected at actual target.
  Fault hooks distinguish target inode/name and ordinal, not global fsync/close.
  Observe exact primary, once firing, no successful receipt/checkpoint, unchanged
  outside catalog sequence/hash and descriptor baseline restored. Any partial/visible
  pair is retained and old explicit catalog cannot adopt it. Stage/final evidence
  existence depends on the named boundary; do not assume every early fault writes.
- W2: all11 markers x stage replacement/racing final/third alias/equal-length bytes/
  building-object insertion, at actual prelink or final fence boundaries. Original
  or foreign evidence remains. Add relevant primary+close OSError/KI combined faults
  with actual release-before-error/FDreuse and secondary notes, not a vacuous monitor.
- W3: all11 markers x real SIGKILL at staged-open/after-write/after-link/before-catalog-
  checkpoint. Dedicated synthetic child first announces SETUP_READY after public imports and
  synthetic catalog loading; parent grants GO only then. Import/setup deadline30s,
  subsequent target READY deadline10s, kill/reap deadline5s, combined diagnostic
  cap1MiB+1. Nonblocking selector+bounded os.read framing, NEVER blocking readline.
  Exact nonce/phase lines and no intermediate publication before GO. Child parks
  after target READY until parent actually SIGKILLs/reaps it; returncode=-SIGKILL.
  All-path cleanup kills only the owned live Popen, reaps it and attempts selector/
  stdin/stdout release once each even after cleanup faults; preserve primary/add
  secondary notes. Timeout/malformed/EOF are failed retained evidence, not retries.
  No source/Git/model child. Retained staged/complete-looking pairs cannot be adopted
  from old explicit catalog or claim successful publication-adapter/catalog/semantic
  terminal completion; no process/descriptor leak. At before-catalog-checkpoint,
  create_pair may already have returned its legitimate single-pair receipt; do NOT
  deny that primitive success. A complete-looking killed pair
  MAY validate CURRENT physical custody against an independently supplied synthetic
  expected receipt; that is NOT prior producer success or containing authority.
  Tests must exercise/retain this distinction rather than demand a primitive rewrite.
- W4: explicit test names/JUnit/transcripts prove proposed finite cells; write a
  separate enumerated cell manifest with scope, actual firing assertions, results,
  remaining axes and whole_row_closed:false. Current generic9/80 exact65 records and
  immutable owner65 copies remain unchanged, not pattern-relabelled as closure.
- W5: independently review exact new tests/evidence and unchanged65 dependencies;
  bind67 public subjects (65+plan+newtest),28 explicit suites and a public-only code
  checkpoint. Run main+extracted28 suites, exact modes/hashes before/after, finite
  syntax. Update datedstatus/RESULTS/ANALYSIS/HANDOFF truthfully; no whole SHIP.

## Definition of done
Each proposed adapter cell has a named actual firing fault/interruption and assertions
of no false receipt/catalog success, no adopted/retried/erased evidence, correct primary
and owned resource cleanup. SIGKILL child really dies and is reaped. All earlier
negative reports and exact65 artifacts remain intact; source/private/science unchanged.
Only scoped qualification accepted; whole9groups/all80 and full semantic/producer/
terminal/recovery/hard-containment cells remain OPEN unless separately proven.
Exact independent review must accept this bounded scope; any uncovered fault is
retained and blocks the affected cell. Lint/type unavailable is NOT PASS.

## Risks and remaining axes
Killed processes may leave no final or a complete-looking unregistered pair; evidence
must persist, not be removed or adopted. Test injection cannot claim server crashes,
remote writers, reboot/failover/backups/quota/retention/restore or universal atomicity.
Ordinal wrappers risk hitting registration/catalog instead of publisher; target inode,
READY and exact firing assertions prevent this. close-after-release hooks must never
retry/reclose a reused FD. Real science payload/schema/raw/role/rejection/terminal
control-flow and all other nine-group axes still require separate qualification.

Read-only metadata shows current user session cgroup root-owned/unlimited, delegated
user@516097.service writable with cpu/memory/pids enabled. This is NOT descendant
containment: same-UID helpers may be able to change memberships. No cgroup/system
settings modified; any active diagnostic needs a separate prospectively reviewed
bounded synthetic-leaf plan and an escape control. No production512/hard disk quota
claimed, no administrator contact. V10terminal; AMALGUMv3exposed calibration ONLY.
