# MSAE controller/owner-approval/recovery qualification — 2026-09-16

## Current decision

**Guarded engineering advanced; whole production launch remains BLOCK.** Controller,
create-once catalog transport, exact approval verification and actual synthetic full
history/scientific/terminal reconstruction are implemented and tested. The user's
approval is the selected trust root: **no separate signing key, signer or administrator
is required**. The owner-origin Markdown mechanism is implemented; actual approval
of an exact production candidate has NOT been obtained. Full declared fault-matrix
qualification, hard containment and whole approval remain incomplete. No independent
scientific result changed; no real experiment or training was restarted.

The user's mechanism-policy message is not approval of this exact candidate, not a
whole SHIP and not authorization for history/source/payload/scoring/training. An agent
must not create its own approval. All production entrypoints still stop unconditionally
with `jpc_whole_qualification_pending` BEFORE real input observation.

## Owner-controlled approval: simple Markdown, explicit provenance

- A prospective [owner-receipt plan](plan-msae-norspan-owner-approval-receipt-v1.md)
  received independent plan-only SHIP. The guarded module and controller now accept
  an owner Markdown receipt without a digital key. Optional Ed25519 compatibility
  remains, but it is NOT a production requirement under the user's selected policy.
- A non-authorizing example is outside the candidate on Jumbo:
  `/jumbo/lisp/f004ndc/tmp/lisplab1/norspan-owner-approval-technical-request-fd_tvwt3/whole-approval.md`.
  It remains **Decision: PENDING**. Its [request binding](../reports/verification/msae_norspan_owner_approval_request_binding.json)
  records exact bytes, subject scope and false authority/launch statuses.
  No source-authority approval has been generated. **Do not approve a blocked
  implementation to waive qualification; no owner action is needed now.**
- After technical whole-implementation qualification passes (all applicable matrix/
  containment obligations and exact independent technical review), a request for that candidate
  can be approved by changing `Decision: PENDING` to `Decision: APPROVE` and confirming
  HERE that the owner personally approved that exact final document. The authenticated
  owner conversation must confirm the FINAL receipt bytes/hash after any edits/proof
  recording. Approving only a candidate manifest must not enroll arbitrary later
  receipt/proof bytes. A changed candidate needs a new exact request.
- Overall canonical/production acceptance requires BOTH technical qualification AND
  authenticated FINAL owner approval (plus all other unchanged gates). It remains
  BLOCK while either is missing; no circular prerequisite and no technical waiver.
  A separate [workflow clarification review](../reports/adversarial/msae_norspan_owner_approval_workflow_clarification_review.md)
  approved this documentation-only distinction; frozen65/code/reviews are unchanged.
- The receipt checks **35 finite production inputs**, strict modes/bytes/SHA256,
  scope/lineage/source-pair/review bindings and actual retained-original fences.
  The **65-subject checkpoint** mentioned in the proof reference is external context,
  NOT an independently runtime-verified65-file table. The approval remains outside
  its subject candidate, preventing a mutable self-reference.
- Runtime verifies integrity and scope, not chat authorship or human-vs-agent writes
  under a shared OS UID. Externally authenticated owner origin is an explicit trust
  assumption, not cryptographic nonrepudiation or same-UID isolation. An agent-created
  proof string, generic caller pin, discovered file, checkbox or SHIP header is NOT
  enrollment. PENDING/REJECT, wrong pins/scope, malformed/duplicate/noncanonical data,
  floats/bools and mixed signed/owner arguments fail closed.
- Recovery now labels `approval_verified` and its authentication method; an owner
  receipt has `signed_approval_verified: false`. Parsing a synthetic owner proof does
  not establish actual owner approval, canonical authority or launch capability.

## Exact65 qualification and reproducibility (engineering only)

| Evidence | Observed result |
|---|---|
| Main27 explicit source-free suites | **665 passed,0 failed,0 skipped**, 717.37s pytest summary. |
| Extracted public-code checkpoint27 suites | **665 passed,0 failed,0 skipped**, 718.02s pytest summary. |
| Owner feature test-first baseline |2 failing requirements /40 deselected before implementation: missing owner module and CLI still requiring digital keys. |
| Focused owner feature suite |42 passed /0 failed /0 skipped,75.16s; includes actual fresh full synthetic owner recovery and resource/guard cells. |
| Public syntax only |41 named public Python inputs compile; lint/type tools UNAVAILABLE, not PASS. |
| Post-run exact identities |195 current/frozen/extracted byte/hash/mode/nlink checks pass after both completed runs. |
| Whole fault matrix / original audit |All9groups and80rows have named bounded evidence/current AST wiring; **every whole group/row remains OPEN**. |

Exact [65-input binding](../reports/verification/msae_norspan_owner_approval_successor_binding.json)
subjects SHA256 `85c1d36c6665fa1f69630400098843b19a9f44c6d89a7c1baacb0c22714b4875`.
[Completed runs](../reports/verification/msae_norspan_owner_approval_completed_runs.json)
record the actual JUnit hashes, roots and separately labeled XML/pytest times.
The [public-code checkpoint](../reports/verification/msae_norspan_owner_approval_checkpoint_binding.json)
archive SHA256 is `eea6e4d14229811e98a0734dc79d4691bdaa8eb4dd76d8f345d48fd2447fd45c`.
It contains65 finite PUBLIC inputs and explicit suite metadata, **not** actual source/
raw/private/payload/state/scratch/Git/credentials/generated test keys or scientific
reproduction/server guarantees. The legacy test is SOURCE ONLY and NEVER collected.

Distinct exact independent [authority review](../reports/adversarial/msae_norspan_owner_approval_authority_review.md)
and [whole review](../reports/adversarial/msae_norspan_owner_approval_whole_review.md)
record their scoped findings; any scoped SHIP is not whole approval. The whole
qualification decision remains BLOCK. The original negative whole49 report remains
an ordinary finite production subject: it is not silently overwritten or elevated to
canonical approval. A future canonical whole-report binding still needs prospective
review and a separately frozen qualified candidate.

The [9-group matrix](../reports/verification/msae_norspan_controller_fault_matrix_progress.json)
and [80-row audit](../reports/verification/msae_norspan_controller_integration_audit_progress.json)
are reconciled against exact65 AST and completed665-case JUnit. Pattern-linked cases
are illustrative evidence, not exhaustive causal closure. Original audit source hashes
remain provenance records: their exact historical bytes have NOT been reconstructed
from the different319-checkpoint before-images. Distinct predecessor61/62
and owner65 classification copies remain. Changed regions publish65 explicit inputs
against the finite319-file public checkpoint (logical earlier bounded release38),
with45 shared bound before-images and29 changed/newly-bound rows. A missing baseline
is labeled unspecified, NOT proof of file creation. Old62 publication is preserved.
The current publication path is in
`reports/verification/msae_norspan_owner_approval_changed_region_publication_path.txt`.

## Implemented guarded integration and limits

- One controller routes actual history, authority, acquisition, preparation,
  verification and recovery. Outside-project immutable create-once catalogs carry
  public expected receipts/raw/four-role private manifests, predecessor digests and
  explicit path/hash/lineage. No discover-latest, adoption, overwrite or initiated
  retry. Publication failure poisons the invocation, preserves evidence and pins.
- Fresh recovery reconstructs config/baseline/access-history/source-authority/phase/
  acquisition/scientific rejection and success terminals on synthetic inputs via
  actual validators, not mocked outcome gates. Git/process/status/transport producers
  remain synthetic. Recovery proves a CURRENT verified observation, not prior success
  or work authorization. Complete scientific terminals use the declared raw exception
  at most once per recovery invocation; failures reset contexts and retain evidence.
  C2 bytes remain opaque custody hashes, never parsed or scored.
- Scratch recovery checks current retained original-root identity, namespace/count
  summaries and observed durability, **not historical descendant bytes**, whose
  digests are absent from original terminal summaries. No initiated/foreign deletion.
- Accessible-history ordinary files/directories stay held across final whole-census
  byte/fingerprint/named/namespace/ancestry fences. Any `.msae_keys` component blocks
  the WHOLE census before opening it, not successful credential pruning. History
  admission cannot turn into sampling; primary faults/FDreuse cleanup are preserved.
- Aggregate public/raw/four opaque private originals remain held; known readers and
  authority/catalog/control/approval transports have repeated actual-path/current-FD
  admission. Observed limits are not kernel hard quotas, universal constructor
  coverage or exclusion of arbitrary concurrent caller allocations. Byte fences are
  finite observations, not an atomic tree snapshot or same-UID writer isolation.
- Local bounded Git-status observations are separate from direct source-network
  counts. A synthetic clean helper executed; protected-content/network/descendant
  containment is UNVERIFIED. Direct source-call0 does NOT mean total network0.
- Jumbo-only client behavior is distinguished from UNKNOWN server reboot/failover/
  backup/quota/retention/restore guarantees. No administrator contact or invented
  server assurance; observed fsync/retention does not prove those guarantees.

## Negative evidence preserved

| Exact milestone | Evidence and interpretation |
|---|---|
| First Q1/Q2 | REVISE:2late catalog,1foreign insertion,1signed receipt-float; actual successor regressions pass, originals retained. |
| Whole49 | BLOCK: depth64history4031→4106 descriptors, masked modes, misleading helper/network wording and dependency omissions; separately repaired. |
| Independent49 run |586pass/2FIFO timeouts; bootstrap-vs-reader cause UNKNOWN. Later reviewed separate READY/import30s and unchanged actual-reader3s phases pass; old failures remain. |
| Candidate59 main/archive |Both INCOMPLETE failures, including injected monitor-close and missing public test dependency. Scoped owned-monitor test repair does not suppress genuine close faults; old archives remain. |
| Whole60 |Main/extracted605pass yet actual aggregate4031→4285 and full typed recovery4050→4104 succeeded; genuine reds retained; strict successor adaptations now stop before overflow. |
| Authority/whole61 |Main/extracted614pass (636.36/647.80s) missed actual authority/review/duplicate-reader FD counterexamples; physical4097 observer-adjusted. Original REVISE/BLOCK remain. |
| Exact62 |Actual-path pair/review/duplicate/each transport repairs:8genuine RED+1guard positive→9GREEN; main623pass634.83s/extracted623pass621.87s,186post-run identitychecks; scoped reader-repair SHIP, wholeBLOCK. |
| Exact65 |Owner-origin receipt mechanism/test successor; no actual human approval inferred from policy message or synthetic receipts.665-pass runs do NOT close the whole matrix or hard containment. |

Earlier NFS publication_metadata/ENOTEMPTY failures and metadata-insufficient
same-length rewrites remain documented. Synthetic reproductions/fences do not prove
historical cause, server-cache explanation, failover durability or absence of loss.
All earlier BLOCK/REVISE/bounded SHIP/checkpoints and failed roots are preserved.

## Remaining work, in mandatory order

1. Close the complete applicable declared fault matrix: eleven markers × write/chmod/
   both file/parent durability/promotion/hash/cardinality/close × kill/substitution/
   extras/combined faults; supervisor setup/drain/limits/kill/reap/descendants/pipe-close;
   four raw promotions/schema/blob IDs; first/midway scientific raw access and drift;
   four role seal edges/partial-next combinations; every malformed/stale/counter/
   duplicate terminal through actual replay; constructor/ancestor/all-close/FDreuse/
   primary-error combinations; full history/process/env/root/training-tree/overlap/
   pedigree edges. No absent applicable cell is declared N/A.
2. Qualify original hard process/allocation/descendant/helper containment. Client
   admission and observed counters are NOT hard512process/aggregate disk quotas.
   A previously observed owner-writable delegated cgroup is a prospective no-admin
   route, not activated/approved containment. Server guarantees remain UNKNOWN.
3. Obtain exact independent technical WHOLE-IMPLEMENTATION SHIP (not overall canonical
   acceptance), prospectively bind the canonical whole report,
   freeze the qualified candidate, and obtain the owner's explicit FINAL receipt
   approval through their conversation/document confirmation. Overall canonical/
   production acceptance requires both; it stays BLOCK until all prerequisites pass.
   No external signer or
   administrator needed, but no agent self-approval or bounded-SHIP substitution.
4. Only then: repository identity/license/family/pedigree/full accessible-history
   overlap/deterministic group roles/cross-role separation/task-label support gates
   → replacement four-role blind payload+controls and independent readiness approval
   → reviewed/frozen/contained calibration+C1 scorer/representations/metrics/inference/
   decision rules, C2 inaccessible → permitted calibration/C1 once and stability →
   exactly one justified G3 branch+case-study precedence → applicable M5/M6 and only
   conditional engineering→25M→100M→1B gates (otherwise M7N/A) → freeze claims/configs/
   representations; C2 once and branch functional/causal/sham/stability/robustness/
   statistical/single case-study evaluation → decision/evidence/manuscript/audits/release.
   Equivocal C1 is no-decision, never negative evidence or training permission.

No real census/history/authority/acquisition/source/raw/private/payload/model/scoring/
GPU/tmux/K2/branch/G3/C2 operations, initiated retries or destructive cleanup occurred.
Original scientific protocol/config/TODO/V10 BYTES remain unchanged (reviewed TODO
mode0644 correction only). V10 remains terminal; AMALGUMv3 remains exposed-source
calibration, NEVER independent confirmation. **Work is uncommitted; no automatic
commit/push. Full task remains incomplete.**
