# Prospective source-authority and approval transport admission supplement v1

2026-09-16. Guarded source-free successor only, no real input or enrollment.
Independent exact61 conclusive red: genuine verify_release completes, a controlled
caller then introduces unrelated descriptors up to4085; genuine source approval
signature passes its11-slot admission, but verify_authority opens the actual
source-authority pair and ordinary review without per-path admission. It returns
success at4097 measured descriptors. The4084/4096 trial is NOT an overflow finding.
Preserve exact61 and all preceding positive/negative records and checkpoints.

Before EACH verify_authority owned authority-pair construction and ordinary review
reader, call the existing admit_observation_fds with the actual absolute path.
Before Controller.acquire's distinct ordinary authority-review re-read, repeat that
admission. Before every from_arguments.read approval transport input, repeat admission
AFTER the existing unconditional qualification guard and placement validation.
The production guard stays first and unconditional, with no bypass capability.
No signature algorithm, receipt schema, approval subjects, custody or science change.
Add this finite public prospective supplement to approval input modes0700.

Red/green must use genuine disposable signatures and actual synthetic authority
receipts. Inject unrelated caller pressure only AFTER a real successful release
verification; never replace verifier, source outcome, admission counter or reader
with success. Observe actual descriptor peaks and restore original baseline on both
stop/success. Test source pair and ordinary review separately (including pressure
introduced immediately before the latter), Controller.acquire re-read BEFORE any
acquisition initiation, and guarded transport-reader construction. Production CLI
must still stop before observing any approval/catalog/source inputs. Keep normal
signed authority and full typed recovery positives and all previous counterexamples.

Bounded known-path operational observation admission only: not a kernel hard quota,
universal constructor coverage, racing-caller allocation exclusion, same-UID
containment or helper/network/server guarantee. Whole9-group/all80 closure, owner
trust enrollment and process/allocation containment remain OPEN. A green count
cannot waive those requirements or authorize acquisition/scoring/training.

Freeze a separate exact62 candidate, repeat explicit26 source-free suites and public
checkpoint reproduction and obtain distinct exact independent reviews. No old report
or frozen candidate is overwritten; original science/config/TODO/V10 bytes unchanged.
