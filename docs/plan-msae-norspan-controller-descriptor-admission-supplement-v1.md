# Prospective history reader descriptor-admission supplement v1

2026-09-16. Guarded source-free successor only; retain all negative evidence.

Independent synthetic depth64 history test observed4106 descriptors despite the
existing4096 conservative ceiling: fixed64 transient headroom was insufficient
for the actual consumer's reopened absolute ancestor chain plus leaf/monitor.
Do not retain that fixed assumption or rebase/exclude history to obtain success.

Before retaining each census child, compute conservative path-dependent headroom
for the actual absolute child ancestor chain, leaf and monitoring/scandir transients.
Before every ordinary or exact-paired history reader opens its absolute ancestry,
repeat current-process FD admission with that path-dependent reserve. This second
check handles caller-owned FD growth after the census yielded the object. Check
credential components before even opening the admission monitor. Operational
admission failure must BLOCK the full history gate and close all owned originals
once while preserving the primary failure; no cleanup/deletion, sampling or retry.

Test both unrelated FD growth before leaf admission and after yielded leaf but
before actual classify/read/sha consumers. Preserve the original depth64/depth65
walker semantics; accounting uses actual path depth rather than assuming relative
depth64 equals absolute ancestry64. All measured reader peaks must stay within4096
or the operation must fail closed before the unsafe opens. Protected/model binary
bytes remain untouched, normal small synthetic history still completes, existing
constructor/close fault tests remain meaningful. Finite monitoring assumes controlled
single-writer/caller observation; it is not a kernel FD quota or arbitrary concurrent
allocation exclusion. Production guard remains unconditional; no real census.
