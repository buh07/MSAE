# NORSPAN typed scientific fault wave3 v2 — prospective test-only plan
2026-09-17. Prospective successor to the retained REVISE v1; independent SHIP required before implementation/execution.

## Goal and immutable boundaries
Extend actual synthetic Controller.execute('prepare') beyond the typed-history prefix.
Use only the existing fresh Jumbo Fixture with disposable synthetic Git/source/keys;
no real source/history/private/blind/model/acquisition/scoring, namespace/cgroup probe,
process census or legacy preparer test import/collection. Original production guard,
production code, protocol/config/TODO/V10 and frozen76 checkpoint subjects remain
unchanged. Existing BLOCKs and all partial/foreign evidence remain preserved.
This plan does not authorize a new production architecture or any scientific gate.

## Grounding and finite cells
Use Fixture.acquire (eight intercepted synthetic Git commands; no source transport),
explicit producer catalog pins and actual prepare, private publisher and fresh recovery.
Scope: 40 named causal cells, not any whole nine-group/80-row closure.

1. Two positive controls: actual synthetic scientific success/seal and scientific
   license rejection, then fresh explicit terminal recovery. Recovery grants no launch
   authority and no past-success inference; no repeated synthetic Git commands.
2. Eight raw-read fault cells: OSError/KeyboardInterrupt at each of four actual
   load_validated_raw OwnedPair.read_bytes calls. Wrap the real raw-loader only to mark
   its dynamic extent; do not replace it or the scientific predicates. Record ordinal,
   path and original receipt/device/inode, completed earlier raw reads and initiated
   raw marker/catalog pin. No rejection/seal/source-ready after IO interruption.
   Fresh explicit recovery/prepare must refuse before a second raw-loader invocation.
3. Two drift cells: create `synthetic-history.txt` (mode0644, ordinary plain text, not an approval
   subject or excluded/private path) BEFORE Fixture.acquire/authority/history. Confirm
   its actual registry file row binds its original SHA256/adapter/extracted-unit count;
   its plain-text lexical units must be nonempty. Mutate
   its same-length bytes at raw read ordinal1 or3, retaining old and new bytes. The actual prepare must refuse
   final success; if it does not, retain RED and BLOCK instead of adapting expectations.
4. Sixteen private-sealing cells: four roles × original stage write/no-replace link
   × OSError/KeyboardInterrupt. Bind exact private directory and original stage inode;
   prior completed role pairs, partial target and later absence recorded causally.
   No terminal success, destructive cleanup, second initiated raw loader or role retry.
5. Twelve terminal cells: success seal/scientific rejection × original stage write,
   no-replace link or actual post-pair catalog snapshot × OSError/KeyboardInterrupt.
   The catalog boundary is the FIRST actual `os.write` to the newly O_EXCL-opened
   snapshot original inode, inside real `_write_snapshot`, after the physical terminal
   pair succeeds. Capture current producer catalog sequence/path/hash immediately
   BEFORE terminal `_insert_final` (not genesis); capture physical stage/final and
   explicit unchanged catalog pin AFTER fault. Open/write identity is recorded.
   Fresh recovery starts ONLY via `CatalogStore.load(path, expected_sha256=pin, ...)`
   using that explicit predecessor. Catalog-write leaves an extra snapshot and must
   refuse directory cardinality; stage-write/link leaves a partial terminal stage
   and must refuse unbound/partial pair inventory. No discover-latest/adoption. A physical terminal pair alone is not producer
   success. Fresh recovery must reject unbound/partial terminal evidence; retain it.

Every cell emits JUnit causal properties in failure-safe `finally` even on RED:
original expected exception/result and subsequent assertions remain visible; FD
baseline restoration is checked after owned operation cleanup, without cleaning
foreign/partial evidence. Evidence serialization must not replace an initiating
exception; use finite JSON-native captured facts, not failing namespace inspection.
Properties: firing boundary/ordinal/original identities,
state/pins, retained evidence, read and intercepted-command counts, FD baseline.
Faults are deterministic and targeted by opened original identities, never basename
alone or stale genesis. Separate initiating exception and propagated wrapper chain;
no claim of universal exception identity. Injections are one-shot, never repair/retry.
Close faults BEFORE release, supervisors/descendants and combined faults are outside
this slice; no after-release result may establish their guarantees.

## Milestones, validation, risks and acceptance
A. Independent plan review; REVISE/BLOCK means no test implementation/run.
B. Test-first new suite only; existing production subjects immutable. Preserve REDs.
C. Run exact bounded suite in tmux, fresh Jumbo roots, ETA approximately3–8min based
   on full synthetic reconstruction; inspect progress, no waiting for scientific jobs.
D. Independently review actual code/cells/JUnit and adversarial firing evidence.
   Publish exact changed region and checkpoint manifests; update synthesis/handoff.
   Existing1172 regressions remain exact76 evidence, not a reproduction of added tests.

Acceptance: all40 declared cells actually fire or positive states reconstruct; no
false successful terminal, no evidence deletion/adoption, no repeated initiated raw
or synthetic transport, FD baseline restored, no model operation/authority. Unknown
or failing guarantee is explicit BLOCK. A new production defect requires a separately
reviewed repair/candidate and new regression, not changes hidden within this test plan.

Whole technical approval/canonical binding/exact FINAL owner receipt remain pending;
then NORSPAN eligibility -> four-role sealed payload -> reviewed C1 -> exactly justified
G3 -> conditional M5/M6/M7 -> C2 once -> release. V10 terminal; AMALGUMv3 calibration.

## v1 review disposition
The original plan and REVISE report remain unchanged. V2 fixes the exact physical
catalog write and predecessor/load oracle, prehistory plain-text drift binding, and
failure-safe causal/FD observations. No implementation/launch occurred under v1.
