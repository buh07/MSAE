# Prospective retained controller aggregate admission supplement v1

2026-09-16. Guarded source-free successor only; no real input or launch authority.
Actual independent synthetic public1+raw4+private4 custody starts at4031 observed
caller FDs, returns success at4285 observed FDs and restores original baseline.
This is a concrete observation-admission defect, not proof of kernel quota failure.
Original depth64 red, aggregate red, old49/59/60 freezes and every report stay intact.

Add one runtime admission helper for a canonical absolute observation path and/or
fixed additional slots. Reject any .msae_keys component BEFORE opening the FD monitor.
For ancestry readers/held pairs/directories, len(absolute.parts)+8 conservatively
covers original '/' + all named ancestors + leaf, monitor/scandir and constructor
transients. Count ALL currently held originals and unrelated caller FDs, repeat
immediately before each actual owned constructor, fail observation_fd_admission if
current count + reserve >4096. No sampling, deletion, rebasing or initiated retry.

Use this helper before every directory/pair constructor in the actual Controller
aggregate custody. Also protect held catalog/finite-release _CatalogReader entries,
original catalog snapshot writer and catalog create/load directory constructors.
Before signature verification admit at least11 slots (3sealed memfds + conservative
8 subprocess/monitor transients). These are guarded high-level observation edges;
standalone primitive/other publisher/supervisor hard-containment axes remain OPEN.
Do not claim universal kernel quota or arbitrary concurrent allocation exclusion.
Every admission/monitor failure must preserve the primary, release all already-owned
originals exactly once, and retain evidence and caller-owned unrelated descriptors.

Write actual red/green regressions for aggregate pressure before first construction
and pressure introduced after one owned construction, plus catalog/finite-release
and signature pressure. Actual baseline cleanup and bounded peaks or failure BEFORE
unsafe subject opens required; no substitute mocked counters for these peak cells.
Keep a normal small aggregate positive and original late-byte/name/alias fault cells.
Monitor-close OSError/KeyboardInterrupt remains operational stop, never scientific
negative. Full explicit26 suites, finite import closure, extracted public-only
checkpoint and independent exact new successor review; no report overwritten.

Add this plan to finite public approval input modes0700; exact new candidate61freeze
is separate. Original science/config/TODO/V10 bytes unchanged. Production guard
remains unconditional; owner trust, whole9-group/all80 fault closure, hard process/
allocation/helper containment and unknown server guarantees remain unresolved.
