# NORSPAN typed history/controller fault wave2
2026-09-17. Prospective test-only plan; independent SHIP before implementation.

## Goal / boundaries
Add causal typed producer/controller/recovery coverage beyond direct JSON adapter
wave1, using the existing fresh Jumbo synthetic Fixture. Production code, guard,
original config/protocol/TODO/V10 bytes are immutable. No real census/status/process
scan, history, source/raw/private/keys/blind/payload/model/network/GPU/tmux/training.
Never collect/import/execute legacy test_prepare_msae_independent_norspan_v1.py.
Fixture census and producers operate only its explicit synthetic project root and
public pinned code/config bytes. Disposable fixture keys are synthetic only and
excluded from all public archives. No owner APPROVE receipt/canonical authority.

## Grounding / approach
Independent read-only inventory selects actual Controller.execute('build-history'):
actual typed current_history_registry.json -> baseline.json -> fresh explicit
CatalogStore.load -> Controller.execute('recover'). Existing Fixture replaces only
process/status/Git producers/storage placement, not state/type/history predicates.
Controller.execute guard is overridden ONLY by Fixture's fresh-root assertion.
Use exact baseline stage and original inode identity; do not reuse unfiltered wave1
Hooks.link (would fire registry) or empty-genesis Direct.assert_no_adoption.
Capture predecessor path/hash/sequence only AFTER registry checkpoint succeeds.
Alternative direct adapters duplicate wave1 and miss typed producer semantics.

## Milestones / finite acceptance
1. Positive typed history/baseline + explicit fresh recovery: current observation,
   source/model authorization false, no source namespace, original FD baseline restored.
2. For OSError and KeyboardInterrupt at baseline stage open, original-file write,
   chmod, first file fsync, no-replace link and original writer close: actual firing
   facts (marker, edge, ordinal, original identity), primary preserved where contract
   promises, retained stages/pairs, no false completed invocation, no raw acquisition.
   A close fault is injected AFTER actually releasing its original descriptor; an
   original FD integer can be reused deliberately and must not close twice.
3. Baseline catalog write/chmod/fsync inside actual _write_snapshot: fault occurs
   after physical pair succeeds, actual catalog-number/original writer binding,
   predecessor pin unchanged, catalog/session poisoned as implementation promises;
   fresh predecessor rejects visible unbound successor, no discovery/adoption.
4. Typed two-control baseline mutation/equal-length rewrite or extra physical object
   blocks fresh recovery; compare immutable hashes/pins and retain foreign evidence.
5. Run exact new suite, independently audit implementation/evidence, run explicit
   approved28-suite regression plus new suites in tmux with fresh Jumbo roots.
   Finite source-only syntax/static identity and public checkpoint reproduction;
   lint/type UNAVAILABLE if tools remain absent, not pass.

Definition of done: explicit named firing cells and positive typed recovery, owned
FD cleanup in named cells, unchanged production bytes, no initiated/raw retry, exact
review and evidence binding. This bounded prefix does NOT close all eleven typed
markers, all catalog/raw/private/terminal/supervisor combinations or any whole group.
All9/80closures remain OPEN pending separately evidenced full acceptance.
No retrospective editing of old failed roots/reviews or auto-adoption/retry/deletion.

## Risks / stop criteria / later gates
A test harness can incorrectly target the prior registry/counter or stale genesis
pin; assert marker/inode/syscall firing facts and capture producer predecessor.
Fresh recovery cannot turn partial/unbound evidence into prior success or authority.
Unexpected fixture path/FD leak/child failure blocks the test slice. Retain every RED.
Whole technical review/canonical binding + FINAL owner receipt remain separate,
then source eligibility->sealed payload->reviewed C1->G3->conditional training->C2
once->release. No new signer/admin contact, no waiver of hard containment.
