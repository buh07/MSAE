# MSAE marker-fault qualification — 2026-09-17

## Decision and approval scope

**Guarded engineering advanced; whole production/scientific launch remains BLOCK.**
The owner approved continuing work. The owner's own Markdown/conversation approval
remains the selected trust root; no external signing key, administrator contact or
administrator storage approval is required under the reviewed client-only contract.
This continuation does not waive technical qualification or enroll the FINAL exact
production approval receipt. The outside request remains PENDING; no owner action
is needed until technical qualification is complete.

The production guard remains FIRST and unconditional: `jpc_whole_qualification_pending`.
No real history, source acquisition, raw/private access, payload construction, model
scoring, GPU/tmux experiment, K2, branch training, G3 or C2 opening was initiated.
Original scientific protocol/config/TODO/V10 bytes remain unchanged. V10 stays
terminal. AMALGUM v3 remains exposed-source calibration, NEVER independent replication.

## What changed

A prospectively reviewed [test-only plan](plan-msae-norspan-marker-fault-qualification-wave1.md)
received [independent plan SHIP](../reports/adversarial/msae_norspan_marker_wave1_plan_review.md).
One new public test suite exercises actual publication adapters, permanent-pair
publishers, registration and create-once outside catalogs, in fresh synthetic Jumbo
roots. Its bodies are synthetic canonical JSON, **not typed scientific markers**.
All preceding65 public/production subject bytes, hashes, modes and link counts are
unchanged. No production implementation edits were necessary for these cells.

The focused final suite passes **431 cases /0 failures /0 skips,32.42s**:

| Explicit scope | Passing cells |
|---|---:|
| Positive actual publisher/registration/catalog control,11 marker names |11|
| Eleven publisher edges × eleven marker names × OSError/KeyboardInterrupt |242|
| Stage replacement, racing final, third alias, same-length byte rewrite, building object × eleven names |55|
| Real mutation primary + close-after-release OSError/KeyboardInterrupt with FD reuse |44|
| Genuine SIGKILL at staged-open, after-write, after-link and before-catalog × eleven names |44|
| Harness release-after-error, selector setup, malformed/timeout/cap/pre-GO controls and unconditional production guard |35|

There are **385 bounded adapter fault/interruption cells**,11positive controls and
35harness/guard cells. Each adapter observation is explicitly named in the
[enumerated cell manifest](../reports/verification/msae_norspan_marker_wave1_cell_manifest.json)
with actual JUnit firing facts (target identity/ordinal, retained names, unchanged
catalog pin or genuine kill result) and exact test-source binding. These are not
pattern links relabeled as whole closure. Passing tests verify assertions of no
false adapter/catalog completion, non-adoption, retention and original resource
release in the named scenario—not arbitrary concurrent interference or all faults.

The paired implementation is tested via actual `publish_json`/`publish_bytes` →
`create_pair` → registration → outside-catalog checkpoint. A killed complete pair
can legitimately verify **CURRENT physical custody** against an independently
transported SYNTHETIC expected identity/known payload; that is not prior producer,
adapter, semantic or canonical authority. Immediately before catalog checkpoint,
the primitive may already have returned a legitimate individual PairReceipt.

## Failed evidence and independent review

The original new suite returned **12 failed /385 passed,31.95s**. Eleven mistakes
expected a stale catalog generation to load after its namespace had advanced;
correct behavior is rejection, without discovery/adoption of a newer pin. The
remaining mistake expected RuntimeBlocked rather than its actual GateFailure
translation at the production guard. The original test source and failed JUnit/log
are retained, not reclassified as a production defect or successful baseline.

Independent review then reproduced427green on the intermediate test source yet
found **eight real counterexamples**: four selector allocation/registration failures
left owned test children/pipes alive, and four real premature-publication mutants
were falsely accepted before GO. The initial [harness BLOCK report](../reports/adversarial/msae_norspan_marker_wave1_harness_initial_review.md)
and original RED evidence remain distinct. All reviewer-owned children were
externally killed/reaped/released. Root corrected the harness with protected
selector setup, explicit no-buffer/no-object pre-GO checks, a shared diagnostic-byte
cap and additional actual fault tests; no production checks were weakened.

The [exact final scoped implementation review](../reports/adversarial/msae_norspan_marker_wave1_implementation_review.md)
accepts only the bounded test-only wave. Independent execution passes **443 cases,
0 failures/errors/skips,33.37s**:431 new-suite cases plus12actual review attacks.
These include all eight retained initial RED stimuli now rejected/released correctly
and four additional actual main SIGKILL selector setup failures. All67current/frozen
identities match before and after; the focused431 manifest/JUnit was independently
audited. Scoped SHIP is NOT whole approval.

Full main and extracted public-code checkpoint runs each pass **1096 cases,
0 failures/errors/skips**, 733.68s and 731.98s
pytest summary times respectively. Their actual case sets match. All431manifest cases
occur in both completed runs; **201current/frozen/extracted identities** and42finite
public Python source-only syntax checks pass after completion. Lint/type UNAVAILABLE,
not PASS. [Completed run binding](../reports/verification/msae_norspan_marker_wave1_completed_runs.json)
records exact JUnit/command hashes, retained roots and separate XML timings. No
technical WHOLE approval follows from these results. A distinct
[completed-evidence review](../reports/adversarial/msae_norspan_marker_wave1_completed_evidence_review.md)
independently verifies both actual1096case sets, each adapter's numeric properties,
201current/frozen/extracted identities before and after its audit and the exact
70-member public code archive. Its scoped SHIP preserves WHOLE BLOCK and both older
reviews; it does not replace the initial harness BLOCK. A separate
[finite public evidence companion](../reports/verification/msae_norspan_marker_wave1_evidence_binding.json)
binds public summaries, source archives, logs/JUnit, exact reviews and only explicitly
named numeric synthetic observations; it is authoritative only after its builder
succeeds, not proof of science/server/containment.

## Reproducibility and earlier evidence

[Exact67 candidate binding](../reports/verification/msae_norspan_marker_wave1_candidate_binding.json):
subjects SHA256 `cf93e5142f914202cac6eab1e00e0cb94372805dce9db1806504f3b1285b4f91`.
Its67finite public inputs are predecessor65 plus the new plan and suite, with28explicit
source-free suites. The frozen binding preserves its original at-freeze pending status;
the [current progress binding](../reports/verification/msae_norspan_marker_wave1_progress.json)
and completed-run report carry the later observations, not production authority. The [public-code checkpoint](../reports/verification/msae_norspan_marker_wave1_checkpoint_binding.json)
archive SHA256 is `d282794eba0ac03ae16919220858c6ec8095e4936fc69019364f0bbb05d70d03`.
It contains public sources/test metadata only, NOT real science/state/raw/private/
payload/source/scratch/Git/credentials/generated test keys. The legacy preparer test
is source-only and NEVER collected/imported/executed. Client fsync returns are
observations, not server crash/backup/recovery assurances. Lint/type tools unavailable,
not PASS. New-source publication is separately bound in
[changed-region metadata](../reports/verification/msae_norspan_marker_wave1_changed_regions.json);
absence from predecessor65 is NOT proof of creation.

The preceding [65-input/665-case checkpoint](status-msae-norspan-controller-qualification-2026-09-16.md)
and49/59/60/61/62 negative/scoped reports, failed roots and archives are preserved.
The generic9-group and80-row classifications still describe exact65 and are NOT
silently overwritten by this separate wave. **All whole group/row closures remain OPEN.**
Earlier NFS publication_metadata/ENOTEMPTY causes remain UNKNOWN; synthetic byte
fences and new green tests do not explain their historical cause.

## Remaining work, in order

1. Qualify typed producer/control flow, raw-access/once-only boundaries, catalog-writer
   syscall/registration/predecessor combinations, private seal, terminal and full
   recovery fault matrix. The385 direct adapter fault cells do not close these axes.
   Before-release kill/reap/selector/pipe failures do not have qualified successful
   release guarantees from the after-release cells in this wave.
2. Establish hard containment for allocations, helpers, descendants, network and
   protected content. Read-only cgroup metadata is NOT containment; no cgroup limits,
   memberships or system settings were changed. Delegated same-UID writable ancestors
   require an escape control in any separately reviewed synthetic diagnostic. A new
   [availability diagnostic draft](plan-msae-norspan-containment-availability-diagnostic-v1.md)
   received [planning-only review SHIP](../reports/adversarial/msae_norspan_containment_availability_plan_review.md).
   It is NOT launch-ready: exact lifetime-stable descendant supervision and minimal
   runtime/mount supplements must pass independent prospective review first. No
   active cgroup/namespace probe or system mutation was performed. Server
   crash/failover/backup/quota/retention/restore guarantees remain UNKNOWN, distinct
   from client-verified limits/retention/fences.
3. Obtain exact independent technical WHOLE approval, prospectively reconcile its
   canonical report binding and freeze the qualified candidate. Then obtain FINAL
   owner approval for that exact production receipt. Canonical acceptance requires
   BOTH technical qualification and authenticated owner approval; neither waives the
   other. The agent must not write its own APPROVE receipt.
4. Complete independent-source repository/license/pedigree/history-overlap/group-role/
   cross-role/task-label/deterministic-split eligibility gates. Only then construct
   and seal replacement payload/controls and obtain independent readiness approval.
5. Freeze/contain/review calibration+C1 scorer and rules with C2 inaccessible; run
   permitted calibration and open C1 once; verify inference/stability and justify
   exactly one G3 branch. Equivocal remains no-decision, not training permission.
6. Freeze M5/M6 and conditional branch work. Only if justified run engineering →25M
   →100M →1B sequential promotion gates; otherwise mark M7not applicable. K2/training
   remain prohibited before this gate.
7. Freeze representations/configurations/claims; authorize/open C2 once; branch-specific
   functional, causal/sham, stability, robustness/statistics and frozen case study.
8. Freeze final decision, claims-to-evidence/manuscript, audits and reproducibility
   release before any later-paper training.

No independent scientific/model result changed. Changes remain uncommitted/unpushed.
