# NORSPAN-1 continuation status — 2026-09-16

> Historical milestone report. For the later guarded controller/authority/history/terminal recovery candidate and current blockers, see [controller qualification status](status-msae-norspan-controller-qualification-2026-09-16.md). Earlier negative evidence is preserved; this report is not current launch authority.

> Newest source-free follow-up: [no-admin client contract/status](status-msae-norspan-jumbo-client-contract-2026-09-16.md).
> This document below is retained predecessor evidence; administrator contact is now forbidden.

**Overall: NOT READY; stopped before acquisition or experimental launch.**
This is a source-free engineering checkpoint, not an independent scientific result,
source rejection, G3 branch decision, or release freeze.

## Latest guarded JPC integration

See the [current JPC integration status](status-msae-norspan-jpc-integration-2026-09-16.md).
Production publication adapters and explicit retained Jumbo scratch reservation/
binding are partially written; recursive initiated cleanup is removed. All public
entrypoints remain unconditionally blocked before real state access. Paired
scientific state/schema/history/authority consumers and full application recovery
remain incomplete. Latest explicit source-free matrix: **379 passed, 6 failed**,
independently reproduced. Late scratch consistency, cleanup/read-boundary faults,
full fault-matrix closure, actual administrator facts and whole/canonical approval
remain open. No real scientific step was started; source eligibility is unknown.

## Earlier Jumbo-only continuation (historical engineering checkpoint)

Statements below about unchanged production and the separate prototype refer to
the frozen predecessor. They do not describe or qualify the newer guarded candidate.

All new artifacts remain on Jumbo. The [JPC-1 qualification checkpoint](status-msae-norspan-jumbo-qualification-2026-09-16.md)
records native storage observations, the separately **plan-only SHIP** v3 design,
corrected 128+128 synthetic NFS diagnostic cases and introduced-holder controls,
an initial **BLOCK** prototype review and test-first successor (**67 native-Jumbo
passes**, compile PASS; independently **Q2-only SHIP**,67+54 reviewed passing cases).
The independent scratch-custody review retains production **BLOCK**.
Initial diagnostic/prototype bugs and their evidence are
preserved; finite positive controls do not establish historical root cause.
New synthetic probes also expose destructive foreign scratch cleanup in the unchanged
runner. A reviewed scratch-preservation solution is required, not retrying deletions.
The prospective integration/no-deletion scratch plan is independently **plan-only
SHIP**; its production implementation and whole qualification remain undone.

The production builder/runner/config/tests/log remain byte-exact and unchanged.
The separate pair primitive does not provide aggregate publication, reviewed recovery,
history/authority/schema integration, full fault-matrix closure, whole-implementation
approval, storage retention/backup guarantees or scientific authority. Old 222 local
passes and bounded SHIP remain prior compatible-local evidence, not Jumbo qualification.
No real acquisition, payload, scoring, training or final operation has begun.

## Work completed

- A separately named, prospectively frozen source/group-role protocol was designed:
  [canonical plan](plan-msae-independent-norspan-v1.md). Its independent
  [design review](../reports/adversarial/msae_independent_norspan_v1_plan_review.md)
  returned **SHIP at plan level only**.
- Added a canonical config, preparation program, exact-sparse acquisition program,
  and synthetic/source-free tests. The candidate's actual source contents, license,
  grouping, support, and overlap outcomes remain uninspected.
- Independent [implementation review](../reports/adversarial/msae_independent_norspan_v1_implementation_review.md)
  returned **BLOCK**. Its reviewed implementation is not acquisition authority.
- A bounded successor repaired destructive public-publisher exception cleanup,
  unknown-binary omission, nonliteral license-URL acceptance, and malformed row/group
  acceptance. Publication failures now preserve obstruction/interruption evidence;
  they do not silently clean the state for a retry. Directory FD identity checks use
  stable object fields rather than mutable directory size/link counts.
- The runtime/control-plane remediation plan was independently **SHIP as a
  source-free plan only**. The newest candidate compiles and has **222 passing tests,
  two N/A publisher skips, on compatible local storage**. Actual synthetic-root
  history/authority, fake-Git acquisition, preparation, terminal success, and both
  acquisition/scientific rejection reconstruction run without mocked authority,
  state, or scientific outcomes. These are not model or external-validation results.
- The copied public-only predecessor subset has **162 passing tests, five deselected**.
  Those five need historical real inode/carryover namespaces and are not marked
  passing or supplied fake identity approval. The old 167-case result remains prior
  evidence, not current full isolated coverage.
- Both runtime reviews returned **BLOCK** and are preserved: the
  [first review](../reports/adversarial/msae_independent_norspan_v1_runtime_remediation_review.md)
  reproduced inode/ancestor/schema/startup defects despite 172 passing tests; the
  [second review](../reports/adversarial/msae_independent_norspan_v1_runtime_followup_review.md)
  reproduced same-inode wrong-byte success and foreign-temp deletion. Its exact full
  NFS suite also had one scratch-cleanup `ENOTEMPTY` failure before a mutation assertion
  (**206 passed, two skipped**); this failure is not called repaired.
- The newest publisher retains original writer FDs and rechecks opaque actual hashes,
  bytes, metadata and read-time change fingerprints. Atomic no-replace promotion
  replaces link/unlink retirement; no publisher erases temporary or foreign objects.
  Racing final objects are preserved; unsupported operation has no overwrite fallback.
- **Current NFS placement cannot run this implementation safely.** Fresh synthetic
  probes on the same filesystem as the project returned `EINVAL` for
  `renameat2(RENAME_NOREPLACE)`; the actual builder failed with
  `atomic_noreplace_unavailable:errno=22` and retained `.control.building`. A fresh
  local `/tmp` probe succeeded. Passing local checks does not cure NFS compatibility.
  No real protocol publication was attempted and no data roots were silently moved.
- The [atomic-publication review](../reports/adversarial/msae_independent_norspan_v1_atomic_publication_review.md)
  retained **BLOCK** for late single-file temporary and private-root extra objects.
  A final-boundary successor now rechecks exact cardinality after streaming/durability
  and closes retained inventory descriptors when initial fstat fails. Its independent
  [final-boundary review](../reports/adversarial/msae_independent_norspan_v1_final_boundary_review.md)
  returned **SHIP in bounded compatible-local repair scope only**, with **55 passed,
  two N/A skips, 167 deselected** plus independent late-mutation checks. This is not
  whole-implementation, production-filesystem, canonical authority, scientific-readiness,
  or launch approval. Every previous **BLOCK** report is preserved; another path cannot
  become canonical acquisition authority by implication.
- Current transcript: `reports/verification/msae_independent_norspan_v1_source_free_checks.log`.
  Exact earlier transcripts are preserved as `..._pre_remediation_checks.log` (99-test),
  `..._pre_bounded_checks.log`, `..._pre_runtime_followup_checks.log` (172-test), and
  `..._pre_atomic_publication_checks.log` (the failed 209-case candidate), and
  `..._pre_final_boundary_checks.log` (the 221-case atomic candidate).

## Runtime issues found and local repairs

| Issue | Local repair / evidence | Disposition |
|---|---|---|
| Ignored files/empty directories and edits could evade frozen history | Descriptor reenumeration of the complete accessible universe, exact state-relative additions, file hashes/unit counts; repeat at authority and last pre-network/raw boundary and terminal replay | synthetic regressions pass; real census unrun |
| Weak metadata/control schemas and stale rejection capabilities | Shared canonical, duplicate/nonfinite-safe exact validators; actual modes/nlink, review hashes, entry/marker lineage and fresh terminal evidence | synthetic chmod/hardlink/schema/counter/history/capability mutations reject |
| Valid UTF8 spanning the 65,536-byte probe could falsely fail | Incremental non-final decoder; full adapter still rejects invalid/NUL bytes | boundary and late-invalid cases pass |
| Temporary-file substitution could publish other bytes while returning the expected SHA | Retain original writer descriptors; recheck actual opaque SHA/byte/metadata/fingerprint evidence before/after atomic promotion and complete publication | regression cases pass on compatible storage; old byte/deletion counterexamples preserved |
| Publisher's checked-then-unlinked temp could erase foreign replacement | Atomic no-replace rename, no retirement unlink or unsafe fallback; preserve racing/substituted objects | racing-final/temp cases pass locally; current NFS rejects the required primitive |
| Late single-file temp/private-root extra objects escaped the final check; initial inventory fstat error leaked owned descriptors | Recheck exact cardinality after complete streaming/durability; put initial root fstat inside descriptor cleanup | final bounded independent review SHIP locally; negative cases retain evidence and close owned FDs |
| Dropped parent descriptors allowed detached named ancestry | Retain all absolute ancestor descriptors; revalidate each named link and root after reads/publication/tree walks; close every retained descriptor on success/error | immediate/nonleaf parent exchange and leak regressions; successor focused tests pass |
| Successful acquisition accepted contradictory flags and malformed nested metadata | Canonical typed literals, exact scratch/file/object records, derived counts and required commit/tree/blob sizes; reconstruct ordered object-inventory bytes against the observed command SHA/count | malformed flags/nested metadata reject; successor focused tests pass |
| Selector initialization after spawn could leak an owned child | Initialize selector first; protect Popen, registration and drain with owned-group kill/reap and selector/pipe closure | no-child selector failure plus actual owned-leader registration and Popen failures pass |
| Raw publication requested 0555 but restrictive umask produced 0500 | Build 0700; set final mode with descriptor fchmod; raw directory and its contents use one pinned FD | restrictive-umask and raw-directory exchange tests pass |
| Private publisher erased partial attempt evidence | Remove exception cleanup; preserve all partial objects and reject retry | applicable create/open/write/fsync/atomic-promotion faults retain obstruction; publishers perform no unlink |
| Private verifier materialized complete actual bytes | Chunked opaque SHA/byte/newline-count verification with mode/nlink/identity checks | no actual-private full-byte read/parse in synthetic verifier |
| Git capture could hang or exhaust memory | Frozen 600s/command, 16MiB stdout, 4MiB stderr; concurrent monotonic supervisor; owned group termination and leader reap | timeout/caps/concurrent-pipe/descendant checks pass |
| Permitted pre-start resume was refused | Reconstruct all controls/reviews/history before resuming entry/ready states only | actual synthetic network/raw pre-start resumes; started states never reenter |
| Terminal success could accept changed claims; rejection did not prove first failure | Freeze/replay literal claim/no-training flags, payload/control schemas and deterministic scientific first-failure/prefix | actual success/license/acquisition rejection and tampering checks pass |
| Operational IO failures could be misreported as scientific negatives | Only deterministic source-format/gate failures may seal scientific rejection; unsupported parse/internal/acquisition custody faults and failed publication stay unresolved | interrupted publisher/raw-start tests show zero repeat access |
| Worktree and Git object metadata were insufficiently cross-checked | Exact object ID/kind/canonical-size/uniqueness, required commit/tree and worktree Git-blob/size matching before raw publication | substitution, missing commit, duplicate/unknown/negative metadata reject |

These repairs do **not** close every failure interval. The initial NFS-backed
synthetic suite had two unexplained strict `publication_metadata` failures; a later
frozen suite hit scratch-cleanup `ENOTEMPTY`. Retained scratch directories were empty
on a subsequent metadata-only inspection, which does not establish a cause. No
metadata predicate was relaxed, attempted object erased, or source operation retried.
Compatible-local checks avoid that filesystem; they do not fix its reliability.
The full combined phase/publication/custody exchange/kill/cardinality matrix remains
unclosed. Native atomic-promotion compatibility is now a confirmed additional blocker.

## Remaining prerequisites, in order

1. **Establish compatible placement and finish source-free implementation approval.**
   Prospectively review a production placement/portability design supporting atomic
   no-replace promotion and explicitly reconcile the changed publication contract
   with the governing hard-link/unlink wording. Do not silently relocate real roots,
   copy protected predecessor bytes, relax custody, or introduce an unsafe fallback.
   Obtain whole-implementation review of the exact successor; close any reported defects, the combined
   phase/publication/custody exchange/kill/cardinality matrix, and the NFS publication
   reliability anomaly. Establish a prospectively approved canonical review/verification
   binding without overwriting retained BLOCK evidence or broadening alias exceptions.
   A separate follow-up report is not itself acquisition authority. Until this closure,
   the real one-way history/authority/acquisition gates remain stopped.
2. **Run the source gates only after a new full implementation SHIP and authority
   review.** Establish license/repository identity, source-family/pedigree, full
   accessible-current-history overlap, cross-role separation, support, and deterministic
   construction. Preserve inaccessible-history gaps explicitly; no claim about all
   bytes ever present is permitted. Do not invent new exclusions or relax generic
   pedigree-identifier collisions after a gate observes them.
3. **Seal the replacement payload/controls only after every preceding gate passes.**
   Independently review readiness and a separately frozen, contained calibration/C1
   scoring implementation before any model operation. That scorer is not implemented
   or authorized by this checkpoint.
4. **Complete independent scoring and sign exactly one G3 branch.** Freeze case-study
   precedence and branch evaluation. An equivocal result is not a negative result and
   cannot activate training or final-test access.
5. **Only then activate applicable branch work.** Learned M7 requires the M5/M6 freezes
   and sequential engineering → 25M → 100M → 1B promotions. Otherwise record M7 N/A
   after the non-learned branch is signed—not while G3 is unresolved.
6. Freeze representations/configurations/claims and open the new final test exactly
   once for branch-specific functional, causal, stability, and case-study evaluation.
7. Freeze the decision gate, claims-to-evidence table, manuscript, and reproducibility
   artifact before any later-paper training. This release milestone remains pending.

## Launch and progress check

- The prior checkpoint lstat-checked real protocol data/provenance namespaces **absent**.
  This remediation did not access or recensus those real namespaces.
- No candidate acquisition, baseline/authority publication, blind payload, model
  scoring, GPU inventory, tmux experiment, K2 restart, or branch training ran.
- The prospectively reviewed plan prohibits GPU inventory/tmux before the source and
  prescore readiness gates. Consequently `nvidia-smi` was **not** used to claim that
  GPUs were free, and no unauthorized experiment was launched merely to use hardware.
- V10 stays terminal; AMALGUM v3 remains exposed-source calibration only. Atlas
  quarantine and predecessor protected bytes were not read or hash-opened.
- No NORSPAN experiment was launched to check or await. The 222 passing checks use
  fresh compatible local temporary and scratch roots, not the project's NFS placement.
  Source-free checks demonstrate local repairs only; full-protocol blockers remain. No claim about general GPU/job
  health is made. There is no experiment completion ETA because no experiment clock
  has started.

## Conditional ETA — no experiment clock has started

Acquisition/history/C1/final-evaluation ETAs are **not yet defensible**: their approved
inputs, exact job matrix, hardware, and measured throughput are not all available.
Readiness repair/review is a dependency, not a scheduled GPU job.

For compute planning only, public historical June fastdata logs have tail-100 median
throughput about **2,550–2,577 tokens/s per job**. Using a rounded **2,400–2,700 tokens/s**
planning range gives:

| Conditional stage | Budget per job | Approximate per-job runtime | Required allocation |
|---|---:|---:|---|
| M7a engineering | 1M–5M | 6–35 minutes | one engineering seed per runnable comparator |
| M7b short gate | 25M | 2.6–2.9 hours | two paired seeds per activated comparator/config |
| M7c medium gate | 100M | 10.3–11.6 hours | three paired seeds per required headline comparator |
| M7d confirmation | 1B, from zero | 4.3–4.8 days | five paired seeds per promoted headline comparator |
| M7f, only if required | planning default 50M | 5.1–5.8 hours per continuation | five paired seeds, zero/held regularizer arms |

Evidence for throughput only:
`pilot_runs/20260601_132158_k2_msae_wave2_fastdata_stream/logs/`.
The historical resumed checkpoints do **not** substitute for fresh confirmatory seeds.
These estimates exclude startup, validation, review delays, failures, and changes in
GPU/model/data configuration; they are not promises for the future architecture.

For illustration only, **five distinct comparators and four equally fast, continuously
available GPUs**, with all required variants promoting, imply roughly 8–9 hours for
the 25M matrix, 41–46 hours for 100M, and 30–34 days for 1B: **about 32–36 days for
those sequential training gates**, before other work or extra activated variants.
Current free-GPU count and comparator count are unknown. A non-learned G3 branch has
no M7 training time. No 8B extension or later-paper training is automatically queued.

After authorization, inventory available GPUs using `nvidia-smi`, run every actual
experiment in tmux, measure early token/record throughput, and report a refreshed ETA
after checking process/log/checkpoint progress. Do not wait for long jobs to finish.

## Reproducibility and next action

Baseline `main` was clean and synchronized with `origin/main` at `75653a7` before this
continuation. The NORSPAN draft, reviews, and checkpoint are currently **uncommitted**;
no new commit or push was made. Verification logs are Git-ignored and need deliberate
inclusion when a reviewed checkpoint is eventually consolidated.

**Next command (read-only, not a launch):**

```sh
cat reports/adversarial/msae_independent_norspan_v1_implementation_review.md \
    reports/adversarial/msae_independent_norspan_v1_bounded_followup_review.md \
    reports/adversarial/msae_independent_norspan_v1_runtime_remediation_review.md \
    reports/adversarial/msae_independent_norspan_v1_runtime_followup_review.md \
    reports/adversarial/msae_independent_norspan_v1_atomic_publication_review.md \
    reports/adversarial/msae_independent_norspan_v1_final_boundary_review.md
```

Resolve the remaining source-free blockers and obtain a full successor review before
running `build-history`, `build-authority`, acquisition, preparation, scoring, or tmux.
