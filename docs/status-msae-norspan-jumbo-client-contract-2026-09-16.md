# NORSPAN — Jumbo client contract follow-up (2026-09-16)

> Historical milestone report. For the later guarded controller/authority/history/terminal recovery candidate and current blockers, see [controller qualification status](status-msae-norspan-controller-qualification-2026-09-16.md). Earlier negative evidence is preserved; this report is not current launch authority.

**Source-free engineering only. TASK INCOMPLETE / WHOLE LAUNCH BLOCK.**
Owner forbids administrator contact; none occurred. Keep all canonical artifacts
on Jumbo. Prepared administrator requests were never submitted and are historical,
not the current next action. This status supersedes the earlier379pass/6fail handoff.
No real history census/authority/source acquisition/raw/private/payload construction,
model scoring/GPU/tmux experiment/K2/branch/G3/C2/release operation occurred.

## Prospective approvals (not launch approval)

The [exact v5 plan](plan-msae-norspan-jumbo-client-contract-v5.md),
[client storage contract v3](contract-msae-norspan-jumbo-client-storage-v3.md) and
separately named `jumbo_client_storage_v1.json` received independent
[plan/contract-only SHIP](../reports/adversarial/msae_norspan_jumbo_nfs_aggregate_plan_review.md).
Earlier v1/v3/v4 REVISE and whole-candidate BLOCK reports remain intact.
The historical v3/contractv2 sentence retained within v5 is predecessor context;
the exact current subjects are v5/contractv3, not those older names.
DES-0231 is proposed only; DES-0203/0221 topology stays active. Old proposed DES-0230
and old config's administrator-required field are not reinterpreted as satisfied.
Original16scientific fields, protocol/TODO/V10-reference bytes unchanged.

## Software and newest evidence

- Scratch now retains all admitted original FDs; full frozen directory/file
  fingerprints, two alias boundaries, different final flat held-directory name/
  original-FD fence. Incremental names reject cap+1;1024objects including root,
  depth64, conservative current-process FD admission under4096. No evidence deletion.
- Supervisor observes leader exit without reaping before group termination;
  independently attempts group stop, bounded leader reap, selector, both pipes.
  Original failure preserved with secondary notes. Failed cleanup cannot publish
  an acquisition rejection/success terminal as if teardown completed.
- Ordinary/producer readers open no-follow/nonblocking before regular-file checks;
  pinned full ancestry, before/during byte bounds, all once-only releases.
  Directory creation handles owner umask without chmod of preexisting foreign dirs.
- Pair create/open/verify/validate boundaries have additional bounded original-FD
  expected-byte fences. Aggregates observe all held expected bytes after sequential
  validations, then frozen metadata/alias/ancestry. Never rebase hashes/receipts.
- Actual paired control context/session, live receipt registration, canonical
  consumers/digests, physical/logical inventory, raw paired/Git-blob consumer,
  opaque four-role custody verifier, retained scratch schemas and phase fields
  are written. Four actual synthetic consumer integration cases pass.
- Explicit guarded `recover` command and typed paired metadata observer are written.
  The observer performs zero source/model work and always reports canonical
  authority NOT_ESTABLISHED, history/science replay NOT_COMPLETED, no work authority.
  It is NOT full approved recovery or scientific replay.

Latest root22explicit-suite run: **477passed,0failed,0skipped**,23.88s.
Evidence root `/jumbo/lisp/f004ndc/tmp/lisplab1/norspan-client-successor-full-n9fwsnkz`.
Prior independently rerun19-suite candidate: **457passed**,29.60s, root
`/jumbo/lisp/f004ndc/tmp/lisplab1/norspan-client-independent-review-dsq4rw0f`.
Its [bounded implementation review](../reports/adversarial/msae_norspan_jumbo_client_implementation_review.md)
returned **REVISE**, not SHIP: eager recovery listdir and whole-control prefix
buffering were genuine independently failing counterexamples. Both are repaired:
recovery now rejects incrementally at finite physical cap+1 with iterator release;
prefix consumers retain only requested output while fully streaming/hash-binding
ALL copied expected bytes and validating original-FD custody before/after. An8MiB
synthetic control's1-byte prefix has measured allocation peak below4MiB in a maintained
case. Independent cleanup4/resource2 donors are now maintained with only relative
import paths adapted. Original donors, RED logs and all34publicbefore-images remain.
New13-case test-first red:10fail/3pass; named green:13pass, then new memory case
and maintained donors included in the477case full run. No failure was skipped.
Exact38publicsubject [successor review](../reports/adversarial/msae_norspan_jumbo_client_resource_successor_review.md)
returned **bounded N1/N2/resource-repair SHIP**, not whole/task/N3 approval.
Independent22-suite rerun:477pass/0fail/0skip24.58s, plus45new independent attacks
pass/0fail/0skip2.26s. Root
`/jumbo/lisp/f004ndc/tmp/lisplab1/norspan-client-resource-successor-review-tqSAHl`.
All38subject hashes/metadata remained exact. Donor annotation "current candidate RED"
is historical donor context; both maintained resource donor tests currently pass.
Green suites and bounded SHIP are not whole fault-matrix closure. Binding: `reports/verification/msae_norspan_client_successor_binding.json`.
Changed-region publication: `msae_norspan_client_successor_changed_region_path.txt`.
Progress: `reports/verification/msae_norspan_client_progress.json`, separately named
and preserving the historical9-group/80-row open reports.

### New NFS finding and retained harness corrections

The first widened repair run had402passed/1failed in unchanged primitive create-3.
100fresh synthetic create-3 rewrites all fired;13were falsely accepted with actual
changed bytes but identical full owned/named fingerprints. A separate100-case
probe retained unchanged times60cases even after successful fsync/statx FORCE_SYNC.
This establishes metadata insufficiency, not a specific cache/server cause,
physical data loss or exhausted storage. Diagnostic roots are preserved via
`msae_norspan_pair_drift_diagnostic_path.txt` and `msae_norspan_attribute_freshness_path.txt`.
After reviewed byte-fence repair,100fresh identical scheduled cases all fired and
**0falseaccepts** (`msae_norspan_byte_fence_native_path.txt`). This is not arbitrary
writer exclusion or an atomic current-state guarantee.

Original7aggregate red cases included2publisher hooks incorrectly matching final
FD paths rather than retained stage FD paths. Those old roots and plan reviews
remain. Corrected maintained hooks recognize stage FD paths, assert firing in
finally, and reproduce7genuineDIDNOTRAISE failures on the extracted frozen two-script
predecessor before passing on current code. Pointer:
`msae_norspan_aggregate_corrected_red_path.txt`. The owned-reader ordinal harness's
postvalidate short injection moved onto a legitimate EOF after adding byte fences;
its old450pass/1fail root and before-image remain. Maintained tests now instrument
actual prevalidate/copy/postvalidate phases; no test is skipped or counted vacuously.

## Bounds and recovery we can assert ourselves

| Property | Actual current assertion | Not established |
|---|---|---|
| Capture |600smonotonic command supervision,16MiBstdout/4MiBstderr; tested owned group stop/reap and no-success-on-cleanup-fault | Absolute wall time during blocked OS/NFS calls; arbitrary escaped processes |
| Reads |8GiB/file cap and64GiB sparse/raw aggregate cap, before/during reads; bounded digest buffers | Hard in-flight aggregate allocation quota/reservation |
| Scratch |Observed8GiB/file,64GiB total,65536entry rejection; stricter1024object/depth64 scanner; existing unrelated FD admission checked | Kernel-wide4096process FD limit; hard quota/process512 enforcement |
| Publication/custody |Exclusive no-replace hardlink pairs, original-FD expected bytes/names; observed successful fsync calls | Atomic simultaneous immutable state; server reboot/failover durability |
| Retention |Production performs no initiated evidence deletion or automatic retry | Administrative deletion policy, backups, disaster restore or retention duration |
| Recovery |Outside pinned expected receipts, canonical finite controls, typed phase/hash/scratch metadata; partial/started-no-terminal stays unresolved/no-retry | Authentic canonical approval from a supplied hash; full history/science/terminal replay |
| Kernel file limit |Fresh8-byte RLIMIT_FSIZE child probe retained EFBIG27 | Production activation, aggregate quota or descendant isolation |

Read-only self-enforcement metadata additionally found the owner's standard
`user@516097.service` cgroup directory, subtree control and membership file writable,
with `cpu memory pids` enabled. Its own current pids/memory limits are root-owned and
unlimited. This is a promising **prospective no-admin per-run cgroup route**, not
qualified child delegation, process isolation or production512 enforcement. No
cgroup directory, limits, memberships or system settings were changed. Free-space
metadata was observed but is neither quota nor reserved headroom. Exact observation:
`reports/verification/msae_norspan_client_self_enforcement_path.txt`. A separately
reviewed bounded synthetic child-cgroup plan is needed before testing that route;
it cannot resolve backup/server guarantees or hard NFS aggregate storage quota.

These are conditional controlled-writer multi-observations. Owned authorized
producers must be stopped/reaped; private0700/locks do not isolate arbitrary
same-UID/remote/server writers. Server/cache/failover/reboot, backup/retention/restore,
restored identities, quota/headroom and hard process containment remain UNKNOWN or
UNVERIFIED. Any unchanged future hard-bound/isolation gate stays BLOCK; no waiver.

## Remaining work / completion audit

N0 prospective v5/contractv3 review: met. N1 repairs: current named tests green,
independent exact bounded successor SHIP. N2 explicit no-admin scope/config: reviewed
prospectively and bounded N2 implementation-qualified; no production activation. **N3 is partial, not complete**:
full create-once controller/catalog transport/lifetime, generic protocol/training
metadata tree admission qualification, exact outside canonical binding,
complete authority/history/all-stage/terminal/recovery validation and end-to-end
science-phase fault cases remain. The deliberate pending canonical helper must not
be replaced by a SHIP filename, observed hash or older BLOCK report.
N4 full declared9fault groups and80original audit rows remain open; test totals do
not close them. Older publication_metadata/ENOTEMPTY causation remains unresolved;
retaining evidence removes initiated cleanup but does not explain all old failures.

Then, in order: independently qualify exact whole candidate/canonical authority →
freeze accessible history/acquire pinned candidate once/source identity-license-
pedigree-overlap-role-support/split gates → seal four replacement role payloads and
readiness approval → reviewed contained calibration/C1 scorer (C2 inaccessible) →
once-only C1 and one justified G3 branch → applicable M5/M6 and conditional engineering/
25M/100M/1B (otherwise M7N/A) → frozen once-only C2 evaluation → release closure.
V10 remains terminal. AMALGUMv3 is exposed-source calibration, never replication.
Changes remain uncommitted; no commit/push requested this turn.

## Reproducible checkpoint

`reports/verification/msae_norspan_client_checkpoint_binding.json` binds a319-file
finite PUBLIC/SYNTHETIC archive, exact extracted file modes/hashes and fresh22-suite
snapshot run:477pass/0fail/0error/0skip24.76s. All319extracted files remained unchanged
before/after tests. Archive SHA256
`1cfff29dbf2ad0fe2ffd30b0ebaedc52ce0a415f376fd455f38eb1447c6fe76e`.
The final proof append/one synthesis pronoun clarification/handoff update follow
archive closure and are declared as a finite three-file public delta; core38reviewed
subjects remain exact. Earlier170-file checkpoint and every BLOCK/REVISE remain.
No broad working-tree archive, key/.git/real-data/raw/private or basetemp included.
This closes checkpoint reproducibility only, not full N3/whole/scientific readiness.
