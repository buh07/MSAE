# NORSPAN namespace containment successor — next-slice plan v1
2026-09-17. DRAFT prospective plan; NOT implementation or launch authority.

## Evidence and goal
One exactly reviewed D1a/D2a launch establishes EACCES self-migration into the configured
Jumbo user@ leaves, but successful CLONE_NEWUSER with changed own handle. This replaces
availability UNKNOWN with two bounded observations, not proof of full containment.
No denied write is retried. No administrator contact, existing-group/ancestor changes,
systemd launch or host fallback is proposed here. Canonical storage stays under Jumbo;
client-only permanent pairs and unknown server/backup guarantees remain unchanged.

Next goal: qualify an actual namespace containment route and lifetime-stable family
custody without relying on unavailable self-migration. The architecture is NOT chosen
by this plan. A reviewed installed bubblewrap route versus explicit no-fork-until-owned
namespace bootstrap must be analyzed before selecting implementation. Kernel facilities,
resource bounds, cleanup and protected-content exclusions are separate obligations.
An available userns does not establish PID/mount/net availability or safe helper setup.

## Prospective milestones and stop conditions
1. Source-only architecture review: exact installed binary/runtime identities and
   helper/fork/setup/exec graph; privilege changes and parent-death resets; original
   pidfd transfer before work; who signals/waits/adopts each declared child. No generic
   process groups, PID/start matching, descendant scans or assumed die-with-parent
   guarantee. Any child born before namespace ownership must have separately proven
   original-handle custody and no uncontrolled-work path. Review decides a route or BLOCK.
2. Freeze exact scripts, args, dependencies, minimal mount/environment/descriptor
   allowlists and controlled-writer assumptions. No host-root/home/Jumbo/project/private/
   keys/cgroup bind as a shortcut; only exact public runtime inputs and owned synthetic
   probe objects. Namespace-owned proc must not expose host descriptors/processes.
   No scoring C2 access; synthetic custody probes do not authorize real role access.
3. Test-first bounded inert family harness: setup/drain/stop/reap, pre- and post-release
   kill/close failures, interruptions, extra members, foreign/reused FDs, substitutions,
   killed supervisors before/after bootstrap and handoff. Prove nonrepeating initiated
   probes, retained evidence and no false successful cleanup. Missing original handles
   or unproven child shutdown BLOCK; no numeric kill/retry/destructive cleanup fallback.
4. Exact independent implementation and frozen launch-binding review BEFORE any broader
   namespace/fork/helper probe. Prior no-fork SHIP does NOT authorize this slice.
5. Bounded facilities/enforcement/escape diagnostics: mandatory PID/mount/net mechanisms,
   runtime-only read view, forbidden synthetic host-content and inherited-FD controls,
   descendants and supervisor death. Classify unavailable /available prerequisite /
   actual enforcement /successful escape /unknown isolation separately. Never a host
   payload when mandatory isolation is unavailable. A negative control must be capable
   of detecting the stated escape; absence of attempted escape is not a PASS.
6. Resource-contract review: distinguish kernel-hard versus sampled/client-observed
   process512, descriptors4096, disk64GiB/helper/descendant/network limits. Per-file
   RLIMIT_FSIZE is NOT an aggregate disk bound; a process-local FD limit is NOT a family
   bound; UID-wide limits/shared host jobs are NOT automatically isolated family custody.
   Unsupported bounds need a prospectively reviewed enforceable architecture/contract,
   not a label change. Keep durable evidence/scratch retention/recovery under Jumbo.
7. Independently audit exact outcomes, publish causal acceptance rows and reproducible
   checkpoint. If any applicable obligation remains unknown, whole qualification BLOCK.

## Definition of done and unchanged scientific gates
Actual containment acceptance needs exact mandatory facility and exclusion/limit proofs,
original-child/descriptor cleanup including before-release faults and relevant combined
faults, no false terminal success/destructive cleanup/repeated acquisition/raw access.
This plan alone closes no row. Typed raw/private/scientific/terminal recovery matrix
remains separate; all9groups/80whole rows OPEN until their own causal evidence closes.

No real history/source/raw/private/payload/model/scoring/GPU/K2/branch/G3/C2 launch.
No legacy preparer test execution/import/collection. No administrator request or external
signer. Final exact whole technical approval/canonical binding/owner receipt enrollment
must still precede NORSPAN eligibility, sealed replacement roles, reviewed C1, justified
G3, conditional training, once-only C2 and final manuscript/artifact release. V10 remains
terminal; AMALGUMv3 exposed calibration only. Existing BLOCK reports/roots unchanged.
