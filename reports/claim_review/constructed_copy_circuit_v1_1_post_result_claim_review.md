# Constructed-copy circuit v1.1 post-result claim review

**CLAIM: SUPPORTED**

The frozen two-panel result supports functional validation of the **exact frozen constructed-copy
v1.1 path only**. Both 256-row generated panels passed every prospective gate; independent
read-only recomputation reproduced the summaries; duplicate live passes were bitwise identical and
agreed exactly with the co-located NumPy oracle.

The result does **not** establish robustness under noisy, soft-attention, learned, or naturalistic
conditions. Its metrics are deterministic and nearly degenerate. The confirmation panel changes
generated rows and token ranges, so it is a second generated panel rather than independent
natural-model replication. No natural prompt, learned representation, SAE, projection, K2 model, or
representation method was evaluated, and no training occurred.

The appropriate scoped conclusion is that the exact evaluator can recognize complete, sufficient,
selective, collateral-free control when it exists by construction. The external validity of this
result and performance of component-discovery methods remain unknown.
