CLAIM: SUPPORTED
SUMMARY: The manuscript supports a scoped encoding-without-separability conclusion and correctly reports the relational prescore stop.

BLOCKERS
  - None.

REVISIONS
  - None required for the current project-level claim. Future relational-object effects must not be
    added unless a new adequately supported opened-development study actually reaches model forward
    and scoring.

EVIDENCE CHECKED
  - `scripts/verify_paper_claims.py` — PASS with 32 claims and 79 exact evidence bindings; all
    quantitative manuscript lines carry known claim markers.
  - `reports/paper_claim_ledger_v1.json` and `PAPER.md` — valid, exploratory,
    technical-invalid, and development-prescore evidence are separated; the paper does not treat
    Attempts 7/9/10/11 as scientific negatives and does not promote Attempt 12.
  - `pilot_runs/20260802_atlas_measurement_v2_4/analysis/results.json` and its independent claim
    review — the eligible K2 result rejects the frozen selective-decomposition conjunction while
    retaining positive token, context, and geometry subresults.
  - `results/atlas_relation_context_v9_attempt13/result.json` — the primary relational endpoint was
    eligible but failed its complete all-task recovery/isolation rule; the manuscript reports the
    favorable dependency-relation values only as exploratory mixed evidence.
  - `results/atlas_context_local_v10_attempt14/result.json` and
    `reports/architecture_program_closure_v1.json` — transferable context capture coexists with
    failure of coherent-versus-unrelated specificity in both directions; no learned comparator was
    trained or claimed.
  - Initial `/adversarial` implementation review — BLOCK on morphology values, intervention
    precision, terminal ownership, readiness signaling, attrition reporting, and reduction order.
    These issues were corrected before any model load or forward.
  - Final `/adversarial` prescore review — SHIP on the morphology-correct build and mandatory stop;
    verified the signed terminal, 77 Latvian components and 196 pairs, the absent opening/result
    namespaces, and the prohibition on third-source substitution.
  - `reports/provenance/relational_objects_v2_development2_prescore_terminal_v2.json` — signed
    `TERMINAL_PRESCORE_INELIGIBLE`; no opening, model-weight load, model forward, endpoint score,
    training, fresh-corpus access, or retry authority.
  - Parser/technical checks — the exact opened manifests pass strict parsing; the parser and harness
    tests report 53 passes; Python compilation, shell syntax, and the manuscript verifier pass.

UNKNOWNS
  - Whether QK links, transported values, or attention-head outputs provide relation-specific
    evidence on a future adequately supported source pair; the current study stopped before model
    inference.
  - Whether separability emerges at other layers, in larger models, or under nonlinear or multilayer
    objects.
  - Whether the positive task-level findings replicate on genuinely fresh scientific corpora.
  - Local hashes and signed lifecycle artifacts establish internal provenance, not independent
    verification of every upstream corpus or model-distribution claim.
