VERDICT: SHIP
ONE-LINE: Public provenance reconstructs an authentic acquisition and honest first-gate license rejection with no payload or neural activity.

BLOCKERS        (must fix before proceeding; empty if none)

REVISIONS       (should fix; not blocking)

NITS            (optional, cap at 5)

CHECKS RUN
  - `sha256sum` and no-follow metadata over all ten public v6 provenance finals → acquisition `49445a3e0e000bc48503b479541b80e319a03a9ae1264a51550f3024a8654c57`; license `cfd3544017eb59eed29e5f3d04e1517955baddfe2bec1c90080bccbea816e29d`; rejection `8b0df4518b349fad9114eaaae62957a6ab0b047232e4bb59b709ad294b03d3f7`; every final is regular, one-link, `0644`, and canonical JSON (apart from the prospectively frozen registry exception).
  - Independent acquisition schema/binding reconstruction → entry SHA `3057f8f5dd0467ae3e922c3560b41842692391899dda33550fb01f8e29cbef50` binds authority `bf7486235379bd8a74608bada43962555bf34cab5ad9836942875bc6ad190fc9`, authority review `50ac9661a88713a517fc92bb7b4aaf2c8acde157d97f060aa65de8a60098cddb`, baseline `16fe87554f9336a19a6355d144b0c823df9a41c4183144148b0aa66a3eff1515`, config `9a299aa464a4c8f0765c3d48bc8e225382a8a03734e5c046eb9ef965efeaec22`, and runner `87df5427acfc5d879eb8546557cb0b7e866f9e9c64ff6e84dadccc811253252f`; the acquisition report repeats the applicable bindings and supplied review hash exactly.
  - Independent command-evidence reconstruction → the 12 commands are in the exact ignore/clone/checkout/head/tree/ls-tree/five-hash-object order, all exited zero, all stderr digests equal empty SHA-256, clone/checkout stdout is empty, and the resolved head is exact commit `5552572ac2c5aac1538e43edb7f7a8d2224f12de`.
  - Tree/blob/output reconstruction from public evidence → tree `081d46572a53f2f53ea6fcca81fc295ade607d97`; the five `100644 blob` records reconstruct exactly 362 bytes and SHA-256 `5e0a72676418f46e17aff5fc1418f29a579b92949190591962793066811da1ba`; every recorded `hash-object` stdout digest reconstructs from the corresponding blob ID, and file/blob maps agree.
  - Acquisition capability/custody audit → sparse-path digest, exact isolated environment, no proxy variables, quiet-output policy, and `git_clone_and_checkout_only` are internally consistent; the scratch repo/home/parent are absent after cleanup. Lstat/directory-name checks only found the exact `0700` v6/raw parents, one exact `0555` commit directory, and five one-link `0444` files whose sizes match acquisition evidence; no raw/source bytes were opened.
  - Gate-order/public-result audit → only `document_group_census.json`, `source_manifest.json`, and `license.json` exist among the 15 ordered scientific artifacts. Census SHA `01bd2e6eebd206d6e4889200760dfaac90a47b80e51f3f339fc3eeeb473c6673` reports zero `newdoc` markers and `eligible`; source-manifest partition hashes match acquisition file hashes; license reports the exact raw/README hashes and `status="ineligible"`; every later family/pedigree/dedup/overlap/support/split/payload/seal artifact and temporary is absent.
  - Independent rejection reconstruction → all 30 ordered scientific final/temp states reproduce byte-for-byte from current public lstat/hash evidence; exactly the three pre-license outputs are present, their sizes/modes/devices/inodes/link counts/mtimes/hashes match, every temporary and later artifact is absent, and `failure_code="license_ineligible"` matches the first ineligible published gate.
  - Full training-root recheck → all 2,116 baseline records and all `139,663,075,787` bound bytes still match their stored SHA-256 values with zero mismatch; `training_root_status="unchanged"` is accurate.
  - Process/model/payload audit → acquisition records zero model/GPU/training operations; rejection records zero builder model/GPU/training operations, false scoring/K2/Stage-C authorizations, and an eligible 39-process point snapshot with zero forbidden identities. The complete private directory, payload final, and payload temporary are absent by lstat, exactly matching `payload_state` and `payload_created=false`.
  - `PYTHONDONTWRITEBYTECODE=1 python3.12 scripts/prepare_msae_independent_source_v6.py verify` → PASS with no output or mutation; the retained rejection reconstructs through the frozen terminal verifier.

CONTRACT COVERAGE
  - Acquisition authenticity and bindings → met — reports/provenance/msae_independent_source_v6/source_acquisition_entry.json:1 and source_acquisition.json:1 form a canonical, exact-hash authority/review/baseline/config/runner/entry chain; public command, commit, tree, blob, exit, and output-digest evidence reconstructs without contradiction.
  - Exact acquired-source custody → met at the allowed metadata boundary — reports/provenance/msae_independent_source_v6/source_acquisition.json:1 binds five exact SHA-256/Git-blob records, while lstat-only inspection confirms the exact closed raw namespace, directory/file modes, sizes, and link counts with no private namespace or retained scratch path.
  - Frozen scientific gate order → met — reports/provenance/msae_independent_source_v6/rejection.json:1 records only census, source manifest, and license as present; all gates after license are absent, proving the builder stopped before source-family, pedigree, dedup, history overlap, support, split, or payload construction.
  - Document census and parser publication → met — reports/provenance/msae_independent_source_v6/document_group_census.json:1 is eligible with zero markers in all three partitions, and source_manifest.json:1 records 1,781/156/153 parsed sentences with hashes exactly linked to acquisition evidence.
  - License result and rejection correctness → met — reports/provenance/msae_independent_source_v6/license.json:1 is canonical, bound to exact acquired LICENSE/README hashes, records `status="ineligible"`, no payload/raw commitment, and no contradictory-term count; rejection.json:1 truthfully records `license_ineligible` as the first failure and requires `new_reviewed_protocol_only`.
  - Scientific artifact inventory and terminal reconstructibility → met — reports/provenance/msae_independent_source_v6/rejection.json:1 exactly matches independent current-state reconstruction for all 30 ordered final/temp entries and payload states; the frozen `verify` command passes.
  - No training/model/GPU/scoring activity → met — acquisition and rejection public records contain only zero operation counts and false authorizations, both stored process snapshots are eligible with zero forbidden identities, and a full independent 139.7-GB hash pass confirms zero training-root delta.
  - Replacement payload/private absence → met — rejection.json:1 records `payload_created=false`, `payload=null`, and two absent payload states; lstat confirms the entire private namespace, final, and temporary are absent.
  - Terminal outcome and next action → met — this is an honest retained scientific failure, not readiness and not independent replication; source scoring, K2/branch training, and Stage C remain unauthorized and v6 cannot be retried under the frozen protocol.
  - Review restrictions → met — no network/acquisition/prepare/baseline/authority operation, raw/source/private/quarantine content read, or model/GPU/scoring/training command was used; data inspection was directory-name/lstat metadata only, and terminal verification took the public rejection branch.

UNKNOWNS
  - The raw-file SHA-256 values and semantic reason that the frozen license-text predicate returned ineligible were not independently recomputed because this review was expressly forbidden from opening source/raw content; public acquisition/source/license hashes and the reviewed builder chain are internally exact.
  - Live upstream Git state and signatures were not queried; authenticity here means reconstruction of the reviewed runner's immutable commit/tree/blob and command evidence, not a new network attestation.
  - Point-in-time process snapshots cannot exclude ephemeral external processes between observations, as the protocol already limits its claim.
