VERDICT: SHIP
ONE-LINE: The revised plan now tests transferable incremental attention-edge information with fail-closed support and lineage.

BLOCKERS        (must fix before proceeding; empty if none)
  - None.

REVISIONS       (should fix; not blocking)
  - None.

NITS            (optional, cap at 5)
  - PLAN_RELATIONAL_EDGE_V1.md:175 — Report marginal confidence intervals as non-simultaneous in the final claim limitations.

CHECKS RUN
  - `/jumbo/lisp/f004ndc/.agent-workspace/bin/check-plan --path PLAN_RELATIONAL_EDGE_V1.md` → PASS.
  - `sha256sum PLAN_RELATIONAL_EDGE_V1.md` → `87c1269a033f15e96ac77bed8cd63b699d522998b116ceb9a784eb442b4573e1`.
  - Rechecked prior BLOCK findings → all fixed: conditional combined-minus-residual gate; exact attention-normalization matching fields; hidden-state-index-4 baseline; source exposure audit; exact natural-surface alignment; fit-source-only alpha selection.
  - Read the prospective gates, decision table, one-shot lifecycle, and tmux/GPU constraints → internally consistent and fail closed.

CONTRACT COVERAGE
  - Preserve and close Attempts 13/14 → met — hash checks occur before preparation/opening and after terminalization; no continuation path is authorized.
  - Accurate encoding/separability paper synthesis → met — exact limited conclusion and learned-comparator prohibition are explicit.
  - Genuinely different relational object → met — eager attention probability/value-transport edges replace token-local branch training.
  - Relation-specific control → met — exact position/gap/length/UPOS/subtoken matching plus combined-versus-output-residual gain.
  - Fresh source/project exposure scope → met — project-wide alias/hash/content audit precedes any freshness claim; Pythia pretraining exposure stays unknown.
  - Support and uncertainty → met — genuine newdoc groups, disjoint components, caps, materialized 500-draw maps, 490 finite floor, and fit-uncertainty limitation.
  - No neural training → met — no optimizer/checkpoint/training is allowed and a pass only nominates later work.
  - Adversarial-before-inference/tmux free GPU → met — exact candidate SHIP, synthetic QA, signed authorization, and bound free-GPU launch are ordered before science.

UNKNOWNS
  - Exact matching may make a source prescore-ineligible; the plan correctly requires stopping rather than relaxing fields.
  - Pythia pretraining overlap with Ukrainian-IU or Arabic-PADT cannot be established from the project artifacts.
