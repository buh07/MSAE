VERDICT: SHIP

ONE-LINE: The frozen count mismatch is real; retained rejection is exact, terminal, source-unscored, and training-closed.

BLOCKERS:
- None.

REVISIONS:
- None. Do not amend or retry v8; any correction belongs to a new prospective, independently reviewed protocol.

NITS:
- `scripts/prepare_msae_independent_source_v8.py:1823-1825` — A successor should derive `hash_object_count` from its exact file tuple, as the v8 runner already does at `scripts/acquire_msae_independent_source_v8.py:853-868`, and add a runner-to-builder integration assertion. This is the diagnosed v8 failure, not permission to alter or retry v8.

CHECKS RUN:
- Rehashed the required public control inputs. The authority review remains SHA-256 `2567d7db6f72b0a19d66ec36bffd274ebb1e59ffb5f55b90fb688f4ed876f3e6` and starts `VERDICT: SHIP`; the frozen builder, runner, config, plan, plan review, implementation review, and authority artifacts remain at their acquisition-bound hashes. No network or executable scientific/model capability was invoked.
- Strict-parsed the create-once acquisition entry, acquisition success, and scientific rejection with duplicate-member/non-finite rejection and exact canonical-byte checks. Their current SHA-256 values, modes, and link counts are:
  - `reports/provenance/msae_independent_source_v8/source_acquisition_entry.json`: `091d8472366f9ac8b01462b49f75f33c25bf77caa1569f947ddeda9e49a4140d`, `0644`, one link;
  - `reports/provenance/msae_independent_source_v8/source_acquisition.json`: `6e57111da0b139bb2c4f0333b230b0a72458d7c419d50cfcb6896596b7f62958`, `0644`, one link;
  - `reports/provenance/msae_independent_source_v8/rejection.json`: `6352d5a52628898cbc1c4f91bbd97dfddf0aff48dd4c1422bc5d82517b33ad52`, `0644`, one link.
- Reconstructed the entry/acquisition chain from public evidence (`source_acquisition_entry.json:1`; `source_acquisition.json:1`). The entry binds the exact authority review, authority manifest, baseline, config, runner, repository, commit, and four paths with all authorizations false. The acquisition binds that entry, the review hash as the supplied gate argument, resolved head `c434778d9511be5c35a6a11531f0107a960fb5d6`, a 40-hex tree object, exactly three `sv_talbanken` CoNLL-U partitions plus `LICENSE.txt`, four tree/blob identities, exact sparse-checkout digest, minimal environment, empty proxy list, and `source_content_printed=false`. This matches the public acquisition contract at `docs/plan-msae-independent-source-v8.md:220-239`.
- Reconstructed all 11 recorded acquisition commands from the frozen config. Every actual command record has exit status 0; every stderr has byte count 0 and SHA-256 `e3b0c442...`; clone and checkout also have empty stdout. The recorded head, tree, `ls-tree -z`, and four `hash-object` stdout byte counts and SHA-256 values reconstruct from public commit/tree/blob metadata without source text. The acquisition scratch, clone, and empty-home paths are absent after cleanup (`scripts/acquire_msae_independent_source_v8.py:828-879`).
- Per the expressly permitted opaque-only exception, streamed the four installed raw files solely into SHA-256 and Git-blob SHA-1 hashers without decoding or printing content. Each current file is regular `0444`, one link, has the recorded size/SHA-256, and reproduces its recorded/tree Git-blob SHA-1. The exact commit directory is the recorded device/inode, `0555`, one directory link pair; its parents are non-symlink `0700`. This establishes internal acquisition/report/raw identity. No semantic source inspection occurred.
- Reproduced the first frozen failure. The runner correctly records seven actual top-level exit-status fields at zero and the informational `hash_object_count=len(FILES)=4` (`scripts/acquire_msae_independent_source_v8.py:853-868`). The frozen builder instead demands the same map with `hash_object_count=5` and immediately raises `GateFailure("source_acquisition_exit_status")` (`scripts/prepare_msae_independent_source_v8.py:1823-1825`). It fails before command-detail validation at line 1826, raw-byte hashing at lines 1887-1909, the document census at line 2260, and all semantic candidate parsing. Thus the code is a control-plane cardinality defect, not a nonzero subprocess exit or a scientific finding about Swedish-Talbanken.
- Strictly reconstructed the rejection's exact 19-key schema (`rejection.json:1`; verifier at `scripts/prepare_msae_independent_source_v8.py:2628-2691`). It records `status=rejected`, the exact first code `source_acquisition_exit_status`, `source_content_reported=false`, `next_action=new_reviewed_protocol_only`, history digest `8070a7cdd536a0dbd6104ff1dfb279ef01fb0acf98e969227625324155227564`, unchanged history/training statuses, zero builder model/GPU/training operations, and false model-scoring/K2-or-branch/Stage-C authorizations.
- Reconstructed the scientific inventory in its literal plan order: all 15 scientific finals and all 15 named temporaries are lstat-absent. Both payload final/temporary states are lstat-absent, `payload_created=false`, `payload=null`, and the entire private v8 directory is absent. Acquisition rejection, acquisition/scientific publisher temporaries, `seal.json`, and `no_training_gate.json` are also absent. This matches the retained-failure contract at `docs/plan-msae-independent-source-v8.md:503-528`.
- Validated the recorded terminal process snapshot schema: 38 entries, exact frozen token vocabulary, every `forbidden_codes` list empty, `forbidden_identity_count=0`, and `status=eligible` (`rejection.json:1`; schema enforcement at `scripts/prepare_msae_independent_source_v8.py:360-382`). This supports only that point-in-time observation.
- Rehashed every current non-private, non-quarantine baseline file before the structural phase, including all 2,116 training-root files, and checked exact baseline metadata/content identity. The four private/quarantine paths were lstat-checked only; no private or quarantine content was opened. Reconstructed the 2,116-entry training array and digest `2e655873dee4824cda59ca18bfbbde3b3f62e86f454860fbf2d1af4235779fc5`. A corrected builder-matching structural recensus found exactly the 17,937 baseline paths plus the six expected public-chain finals and four exact raw files—no extra scientific, payload, or training path. The initial recensus attempt incorrectly excluded nested `tests/.pytest_cache`; it failed only after the full hash phase and reported exactly those six already-baselined paths as missing. Re-running only the recensus with the builder's root-prefix rule passed with 17,947 current paths and exactly ten additions; this was a reviewer-script correction, not artifact drift.
- Checked the complete authorization chain across authority manifest, acquisition entry, acquisition report, and rejection: all model/K2/branch/Stage-C authorizations are false and every recorded builder/runner model, GPU, and training operation count is zero (`preacquisition_authority_manifest.json:1`; `source_acquisition_entry.json:1`; `source_acquisition.json:1`; `rejection.json:1`).
- Verified terminal non-retry behavior statically. `build_ready()` rejects any existing rejection/seal/no-training terminal before scientific work (`scripts/prepare_msae_independent_source_v8.py:2234-2243`), while the acquisition runner accepts the already-valid success report only idempotently and with no acquisition subprocess (`scripts/acquire_msae_independent_source_v8.py:559-575`, `scripts/acquire_msae_independent_source_v8.py:804-817`). Rejection publication is create-once (`scripts/prepare_msae_independent_source_v8.py:2306-2340`) and terminal verification returns after reconstructing the rejection branch (`scripts/prepare_msae_independent_source_v8.py:2628-2691`).

CONTRACT COVERAGE:
- **Exact authority/acquisition binding** -> met — review `2567d7db...`, entry `091d8472...`, acquisition `6e57111d...`, frozen config/runner hashes, commit, tree, four files, command evidence, raw opaque identities, and scratch cleanup all reconstruct.
- **First frozen failure and gate order** -> met — public status evidence contains no nonzero command exit; the only mismatch is runner count 4 versus builder's frozen literal 5, reached before semantic source parsing or any scientific artifact (`scripts/prepare_msae_independent_source_v8.py:1823-1826`, `scripts/prepare_msae_independent_source_v8.py:2234-2260`; ordered contract at `docs/plan-msae-independent-source-v8.md:294-304`).
- **Honest retained rejection** -> met — canonical rejection `6352d5a5...` names the real frozen gate code without claiming source ineligibility, records no source content, and exactly binds the observed empty downstream state.
- **Payload/scientific containment** -> met — all 30 scientific final/temp paths and both payload paths are absent by current lstat; no payload or scientific output exists.
- **History/training/process containment** -> met — current baseline hashes/metadata, exact training-root array/digest, terminal eligible snapshot, zero forbidden identities, and zero builder/runner neural operation counts reconstruct, subject only to the review-content prohibition stated under UNKNOWNS.
- **Terminal/no retry** -> met — the retained rejection blocks prepare re-entry, the acquisition success is idempotent rather than reacquired, and `next_action=new_reviewed_protocol_only` correctly requires a successor protocol. V8 grants no training/scoring authorization (`docs/plan-msae-independent-source-v8.md:616-632`).

UNKNOWNS:
- External upstream authenticity was not re-queried because network access was prohibited. The narrower internal claim is verified: captured Git command metadata, installed opaque bytes, tree blob IDs, and exact commit bindings are mutually consistent.
- The coordinator reports that the frozen builder's `verify` command exited 0, but no separate post-terminal public transcript exists to hash. I did not rerun it because the review forbids private/quarantine access and executable protocol work; instead I independently reconstructed its public rejection schema/state, current structural state, and all permitted content/metadata bindings.
- The exact private-path `data/atlas_v1/private/final_manifest.json` was lstat-checked but not content-rehashed, and the three Atlas quarantines were also lstat-only, as required by this review's prohibition. The frozen rejection reports its broader builder-side history check as unchanged; this review makes no independent claim about those prohibited bytes.
- No semantic statement about Swedish-Talbanken's license, pedigree, overlap, support, split suitability, or scientific independence follows from this rejection: the protocol stopped before those gates. A corrected attempt must use a new source/namespace or otherwise follow a newly approved prospective plan; v8 itself must not be retried.
