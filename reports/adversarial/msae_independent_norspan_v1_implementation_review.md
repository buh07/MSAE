VERDICT: BLOCK
ONE-LINE: Publishers erase pre-existing evidence, and history, literal source gates, and terminal reconstruction still cannot authorize acquisition safely.

BLOCKERS
  - [critical] scripts/prepare_msae_independent_norspan_v1.py:284-342,620-660 — create-once failures delete objects not created by the invocation.
    reasoning — after `os.mkdir` reports an existing final directory, `publish_flat_directory` enters its exception handler, opens that existing directory, unlinks every child, and removes the directory. `publish_bytes` detects a pre-existing `.building` object, then unconditionally unlinks that object in its exception handler. Source-free counterexamples against builder 3dd33ff0... raised `create_once_exists` but reported `EXISTING_DIRECTORY_SURVIVES False` and `EXISTING_TEMP_SURVIVES False`. Directory/file cleanup also does not maintain an exact ledger of owned inode identities before unlink/rmdir.
    impact — the supposedly no-replace publication primitive can destroy a retained raw final or interruption marker and turn an obstructed state into an apparently cleaner state, violating plan lines 138-159. A green ordinary publication test does not make this safe at a one-way door.
    fix — distinguish successful exclusive creation from preflight failure; preserve every entry-observed final/temp/directory. Track current-invocation object identities, compare them immediately before any permitted cleanup, and parent-fsync cleanup. Preserve partial published finals as terminal/unresolved according to the frozen state machine. Add preservation and exchange/fault regressions for all three publishers.
  - [critical] scripts/acquire_msae_independent_norspan_v1.py:45-77,196-237; scripts/prepare_msae_independent_norspan_v1.py:2005-2038 — the required final accessible-history recensus does not occur before network or reconstruct new paths afterward.
    reasoning — `validate_authority` verifies the registry file's hash, controls, and process/training/porcelain snapshots, but never validates or descriptor-reenumerates the registry's historical files. The later registry validator iterates only its saved entries; it cannot detect an added public file. `protocol_inventory` enumerates only the protocol's public/data namespaces, not repository history. An ignored public addition outside the four training roots, or an edit to an ignored historical file between baseline and acquisition, can leave Git porcelain and the registry-file hash unchanged. The source-identity gate can therefore run against a stale universe after the first network operation.
    impact — plan lines 68-100 require an exact path/hash recensus with only state-dependent additions and a last pre-subprocess/raw check. The implementation cannot substantiate “all policy-accessible current history frozen immediately before acquisition” or reject candidate aliases introduced during the gate interval.
    fix — reconstruct the descriptor-bound complete current inventory before authority, before the first network/raw operation, and at terminal verification. Require exact equality to baseline plus the literal state-dependent authority/self-final table, including ignored public additions and disposition metadata; hash only content-readable entries and never protected bytes. Bind the resulting manifest/digest into each phase marker. Add synthetic ignored-addition, same-path-change, exact-authority-addition, and no-prefix-exemption regressions.
  - [high] scripts/prepare_msae_independent_norspan_v1.py:1210-1231 — unknown public binaries are silently omitted instead of failing the history gate.
    reasoning — outside the exact model-suffix exclusion, any unknown file whose prefix contains NUL or is not UTF-8 becomes `binary_excluded`. A synthetic `unknown.dat` containing NUL deterministically returned that adapter; its content then receives no hash or extracted units. This is a broader content-derived exclusion than the frozen model/checkpoint and protected-path exclusions.
    impact — plan lines 53-66 require any other unclassifiable/unreadable regular public file to make history ineligible, not silently broaden the accessible-history gap.
    fix — retain only the exact reviewed model/checkpoint exclusion universe. Raise a terminal history-classification failure for unknown NUL/non-UTF-8 files and unsupported containers; include extension-lookalike and binary-with-hidden-text regressions.
  - [high] scripts/prepare_msae_independent_norspan_v1.py:1305-1306,2211-2215 — initial public-directory creation and raw-directory enumeration still bypass the ancestor-safe descriptor contract.
    reasoning — `PROV.mkdir(parents=True)` and path-based `chmod` can follow an exchanged/symlinked ancestor before the subsequent no-follow publisher refuses the path. `load_validated_raw` separately calls pathname-based `os.listdir(RAW)` rather than enumerating the pinned directory used to authorize its children. The root open race found earlier in this review was amended at lines 681-729, but these independent pathname operations remain.
    impact — the first authority operation can create/chmod a directory outside the pinned namespace, and raw cardinality is not observed through the authorized directory identity. Plan lines 138-144 reject ancestor exchange and require every ancestor walk to use directory FDs.
    fix — create/chmod the public directory through a retained parent FD with exclusive creation and identity checks; enumerate and open all raw children through one pinned raw-directory FD. Check the named directory still denotes that FD before accepting the observation. Add synthetic ancestor-exchange/symlink tests at both boundaries.
  - [high] scripts/prepare_msae_independent_norspan_v1.py:1574,1602-1608 — license URL matching broadens the prospectively frozen exact URL predicate.
    reasoning — the implementation searches NFKC/casefolded text for approved URLs, although only the whole-token phrases and contradictory identifiers were allowed those normalization rules. A synthetic fullwidth-punctuation string `https\uff1a\uff0f\uff0fcreativecommons\uff0eorg\uff0flicenses\uff0fby-sa\uff0f4\uff0e0` returned eligible with zero accepted phrase matches and one URL match. Its original bytes contain none of the exact URLs in plan lines 169-181.
    impact — a source license that fails the reviewed literal gate can be accepted after exposure. This is protocol drift, not evidence about the candidate's actual license, which remains uninspected.
    fix — recognize accepted URLs and external boundaries on the original strict UTF-8 decoded text with the exact approved ASCII spellings. Apply NFKC/casefold only to the separate token-phrase rule. Add fullwidth, case, punctuation, adjacent-URL-character, and chunk-boundary negative goldens.
  - [high] scripts/prepare_msae_independent_norspan_v1.py:1438-1439,1460-1466 — strict CoNLL-U/group parsing accepts malformed rows and non-boundary newdoc markers.
    reasoning — any row ID containing `-` or `.` is counted and skipped without validating range/empty-node grammar or row consistency. A synthetic `garbage-range` row was accepted as a multiword row. A newdoc marker calls `flush()` when token rows are still pending; a synthetic marker immediately after an integer token, without a sentence boundary, was accepted as a second group. Plan lines 213-218 require the exact marker at a sentence boundary and terminal failure on malformed grouping.
    impact — malformed source structure can pass the first source gate and change document grouping, census, and role assignment rather than cause the prospectively frozen rejection.
    fix — validate canonical integer/range/empty-node IDs and the structural requirements for skipped rows before counting them. Reject newdoc when a sentence is pending instead of manufacturing its boundary. Test malformed IDs, ranges, empty nodes, marker-inside-sentence, comments-after-token, and orphan/duplicate group cases.
  - [critical] scripts/acquire_msae_independent_norspan_v1.py:57-77; scripts/prepare_msae_independent_norspan_v1.py:2041-2208,2425-2470 — authority and terminal reconstruction remain incomplete, especially acquisition rejection.
    reasoning — acquisition control validation compares the recorded mode/nlink to constants but hashes the current file without checking its actual reviewed mode/nlink; `sha_file` at lines 191-213 checks regularity and identity, not those expected values. Authority repo/commit/files/status/zero-operation/absent-field schemas and the full acquisition-entry key set/status/runner/review/subprocess fields are not reconstructed exactly. On acquisition rejection, `cmd_verify` checks three lineage hashes and compares the rejection's retained evidence to retained baseline evidence, then emits `verified_rejection`; it does not validate the reviewed authority/control/review chain or take a fresh process/training/porcelain snapshot. It also accepts status/counters without the exact rejection schema, next-action vocabulary, or supported first-failure reconstruction. Scientific rejection checks prefix hashes but not canonical artifact schemas/status-to-failure alignment.
    impact — a chmod/hardlink drift can pass the acquisition gate, and a stale or incompletely bound acquisition rejection can be declared verified despite current forbidden activity or training delta. The verifier does not implement plan lines 127-136 and 161-167, which are central to honest terminal closure.
    fix — share strict canonical schema/metadata/lineage validators across acquisition, preparation, and terminal verification. Validate actual control modes/nlinks and every critical entry/authority field. Reconstruct each rejection's exact allowed prefix and supported failure/status semantics from public evidence, then bind/check fresh eligible process/training/porcelain recensus without network or protected reads. Add successful and rejected end-to-end verifier mutation tests, including altered metadata, authority bits, failure code, prefix content, retained process evidence, and current training/process drift.
  - [high] tests/test_prepare_msae_independent_norspan_v1.py:265-286,552-613; reports/verification/msae_independent_norspan_v1_source_free_checks.log:32-44 — the mandatory one-way fault/restart/terminal matrix is still absent.
    reasoning — the explicit cumulative-state test ends at `network_started`. The fake-Git acquisition test replaces `validate_authority` and `classify_protocol_state` with assumed outcomes; it never invokes actual prepare/terminal verification. No test invokes `cmd_verify` or `cmd_prepare`, or injects failures/kill-equivalent interruption at phase markers and publisher create/write/fsync/link/unlink/parent-fsync intervals. The 79 green cases leave the deletion counterexamples and literal-gate failures above undetected.
    impact — plan lines 157-159 require those tests before opening the one-way source gate; the transcript's green coverage is insufficient to authorize it.
    fix — add real synthetic authority/history roots, fake-Git acquisition followed by prepare/verify, and the full deterministic fault/restart/cardinality matrix. Preserve objects and assert zero repeat subprocess/raw opens on unresolved/rejected reentry. Require tests for every blocker above before an independent successor SHIP review.

REVISIONS
  - [medium] scripts/acquire_msae_independent_norspan_v1.py:183-195 — reviewed safe pre-network restart states are refused rather than resumed.
    reasoning — `acquisition_entered` and `network_ready` fall into `acquisition_invalid_state`, whereas plan lines 149-151 permit entry plus pre-network-ready restart to proceed before any network-start marker.
    impact — this is fail-closed, but does not implement the frozen recoverable state table.
    fix — reconstruct and safely resume only the explicitly recoverable pre-start states, or prospectively narrow the plan before any entry. Never resume `network_started`.
  - [medium] scripts/prepare_msae_independent_norspan_v1.py:1968-1986 — private custody replay materializes and compares complete payload bytes instead of using the literal digest/count-only private-object comparison.
    reasoning — the actual private file is read into memory and compared with `actual != expected`; no semantic JSON parse or human exposure was observed, but plan lines 165-167 specify only independently reconstructed SHA-256/bytes/record count/mode/nlink comparisons.
    impact — the implementation should make its opaque-read boundary unambiguous and avoid an unnecessary full second private copy.
    fix — use a descriptor-pinned streaming SHA/count verifier with expected identity/mode/nlink and bounded reads; retain only hashes/counts, not private content, outside the publisher.
  - [medium] scripts/acquire_msae_independent_norspan_v1.py:29-33 — Git subprocess capture has no timeout or output bound.
    reasoning — `subprocess.run(..., stdout=PIPE, stderr=PIPE)` buffers unbounded remote/Git output and can hang indefinitely after durable network entry.
    impact — resource exhaustion or interruption produces an avoidable unresolved one-shot execution.
    fix — freeze command time/output bounds and a bounded streaming supervisor before acquisition; preserve initiation evidence and fail closed without retry.
  - [medium] reports/verification/msae_independent_norspan_v1_source_free_checks.log:13 — the durable log binds builder 006fc7a1..., while the final frozen reviewed builder is 3dd33ff0....
    reasoning — the root-directory descriptor amendment landed during review. I reran compile and all 79 source-free tests against the final frozen builder, but did not modify the existing log.
    impact — the old transcript is not a hash-bound verification transcript for this final candidate.
    fix — after all blockers are corrected, regenerate a source-free log with exact final inputs and obtain a fresh independent implementation verdict. Do not use this BLOCK report or the old transcript as acquisition authority.

NITS
  - None.

CHECKS RUN
  - Public input SHA-256/mode verification: plan 7e063dcac878623c7f1f293b77294e4c3d006d839d8df184f6814157fe17d7c9 (0644); plan review f6a24f10d1a95c1687a81f5cd472c56c0c7ea119b671c558582955c8972d24dc (0644); config 21964577964c15087ac0b4eee96dbc2ae3e7e5dcdc0442aa10c205de674c300c (0644); final frozen builder 3dd33ff0ce5f05768af19b252c397cb00d21910011aef066a6b60730f03bd91b (0755); runner 447856a730cda54ef37898059bb11655ca38dbd0fe0043593a56fe39034155cc (0755); tests 36b887eb841b3020601f64f4e73b92ba4b02a2f7302542deacb51cbf5560ee99 (0644); source-free log 5a77576f170159268de068073c9b5d93a779365f194cb54c0f760a7c0d2b2b80 (0644).
  - `PYTHONPYCACHEPREFIX=/tmp/norspan-third-final-pyc python -m py_compile scripts/prepare_msae_independent_norspan_v1.py scripts/acquire_msae_independent_norspan_v1.py tests/test_prepare_msae_independent_norspan_v1.py` -> PASS against final frozen builder.
  - `PYTHONDONTWRITEBYTECODE=1 pytest -q -p no:cacheprovider tests/test_prepare_msae_independent_norspan_v1.py` -> 79 passed in 1.15s against final frozen builder. Earlier run against 006fc7a1... also passed 79 cases.
  - Synthetic existing-directory/temp publication tests against 3dd33ff0... -> both prior objects were deleted while `create_once_exists` was raised.
  - Synthetic unknown-binary, fullwidth-license-URL, malformed-range, and mid-sentence-newdoc tests -> silently excluded binary; nonexact URL eligible with phrase count 0/URL count 1; malformed range counted as MWT; non-boundary marker accepted. Those functions are unchanged between the initial tested builder and final frozen builder.
  - Initial synthetic root-directory exchange test against 006fc7a1... -> accepted a saved root identity with replacement children. Final frozen builder now opens the root relative to retained parent and checks fstat identity (lines 681-729); that specific initial root-open defect is no longer asserted as a current blocker.
  - Re-read prior BLOCK report and explicitly checked each finding against the revised plan/config/programs/tests; no broad repository/history scan occurred.
  - Scope audit -> no candidate data/provenance namespace, V8/V9/V10 raw/source/private bytes, Atlas quarantine/private content, network, acquisition, baseline/authority/prepare/real terminal command, model/tokenizer, GPU inventory, scoring/training/K2/branch, or tmux operation was accessed or invoked. Synthetic files existed only in temporary directories; pycache was external and pytest cache disabled.

CONTRACT COVERAGE
  - Prior BLOCK 1, mixed-disposition history consumer -> met locally — adapter-first dispatch and the protected-directory/mixed compute regression fix the deterministic crash; no real history was read.
  - Prior BLOCK 2, descriptor/history/quarantine/archive coverage -> partial — expected file identities, exact quarantine lstat paths, total archive-member counting, and the root-open amendment improved; unknown-binary omission, missing exact current recensus, and residual pathname boundaries remain.
  - Prior BLOCK 3, canonical review/exact authority -> partial — exact SHIP first-line parsing and the full control-path table are present; actual metadata and full authority/entry/terminal schemas are not reconstructed.
  - Prior BLOCK 4, exact sparse acquisition/replay -> partial — sparse-before-checkout, local blob inventory, fake-Git path, exact command argv and selected output replay are present; failure authority/reconstruction and recoverable restart gaps remain.
  - Prior BLOCK 5, cumulative state/recovery/terminal -> partial — unexpected empty directories/temps and raw-started reentry now fail closed; exact terminal schema/evidence reconstruction and the required fault matrix remain unmet.
  - Prior BLOCK 6, payload/no-training/custody -> partial — final evidence now follows payload publication and same-size substitution is detected; publishers can erase pre-existing objects and private replay should use the literal opaque digest/count boundary.
  - Prior BLOCK 7, complete candidate identifier universe -> met locally — optional URL forms, commit, and all four filename/stem forms are frozen and every identifier has an exact-stop test.
  - Prior BLOCK 8, capability evidence -> partial — expanded launcher/environment vocabulary and training content digests exist; acquisition-rejection verification lacks fresh evidence and metadata/custody boundaries remain incomplete. Point-in-time snapshots are not proof of absent ephemeral activity.
  - Prior BLOCK 9, order-sensitive group normalization -> met locally — NFKC/casefold and boundary whitespace regression are present; separate strict marker/row parsing defects remain.
  - Prior BLOCK 10, source-free tests -> partial — 79 cases add useful pure-function/fake-Git coverage, but do not exercise actual prepare/verify or the mandatory publisher/restart fault intervals.
  - Prior revisions, config and scratch cleanup -> partial — config now contains substantially more frozen controls/containment/decision/capability tables and runner scratch cleanup is descriptor-relative; public/raw/private publisher preservation and exact schema validation are not closed.
  - License/strict parsing/history/overlap/support before model scoring -> partial — the pure scientific order and overlap-before-role/support logic exist, but literal source gates and acquisition history authority remain unsafe.
  - No pre-gate model/GPU/scoring/training/K2/branch/tmux operations -> met for this review and inspected source-free programs — none ran, and this verdict authorizes none.
  - Independent-source/replacement-payload outcome and protocol launch readiness -> unmet — no candidate scientific evidence or payload was acquired/created by this review; NORSPAN-1 remains NOT READY and blocked before one-shot history/acquisition authority.
  - Scientific claim boundary -> met — v3 remains exposed-source calibration, predecessor/V10 failures are not independent replication/model evidence, and the plan distinguishes maintainer independence from model-pretraining/content independence.

UNKNOWNS
  - Actual NORSPAN repository/tree/content, license, document markers, support, overlap, and resulting role counts remain intentionally uninspected.
  - Candidate data/provenance absence was not independently lstat-checked because this review was prohibited from accessing those namespaces; the supplied source-free transcript reports absence.
  - Current process/training eligibility was not queried; no GPU inventory, live scoring/training/model query, or real gate operation ran.
  - Later prescore/readiness review, bwrap containment, G3 signatures, C1/C2 inference, payload-open accounting, and M5-M10 launches are outside the reviewed source-free implementation and remain unauthorized.
