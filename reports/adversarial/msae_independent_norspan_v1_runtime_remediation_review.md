VERDICT: BLOCK
ONE-LINE: Local regressions pass, but publisher inode/ancestor custody, successful acquisition schemas, and supervisor startup cleanup remain unsafe.

BLOCKERS
  - [critical] scripts/prepare_msae_independent_norspan_v1.py:351-362,689-701,2014-2029 — publishers do not bind publication/unlink to the originally opened temp inode.
    reasoning — `publish_bytes` closes the write descriptor before linking and compares the final only with the current temp name; flat/private publishers save the descriptor stat but replace it with a post-link pathname stat without comparing identities. Independent synthetic substitution immediately before `os.link` changed the temp inode while preserving size/mode: single publication returned success with `xxxxx` instead of `alpha`; flat publication returned alpha's SHA-256 while the final contained `bravo`; private publication returned its expected manifest while the discovery final contained 764 substituted `x` bytes. Each publisher also unlinked the replacement temp it did not create. The first flat test retaining the old temp inside the final directory correctly failed cardinality; moving that retained owned temp outside the final tree exposed the independent inode defect.
    impact — create-once custody can publish another object's bytes and erase its temp name while returning original-payload evidence. This violates the owned-object/preservation contract and the M1 same-size-substitution acceptance independently of whether later replay detects corruption.
    fix — retain original file descriptor/device/inode identities through publication; validate named temp and final against that exact owned inode before accepting link/unlink/final metadata, never only against each other. On drift, preserve all evidence and remain unresolved. Add original-inode substitution tests for all three publishers, including cases with otherwise valid cardinality.
  - [high] scripts/prepare_msae_independent_norspan_v1.py:271-297,669-703,2542-2565 — no-follow opens discard ancestor bindings, and final checks validate only a potentially detached parent.
    reasoning — `_open_parent_fd` closes each ancestor as it descends, without retaining/revalidating the named chain. Independent synthetic parent exchange during `publish_bytes`' first write returned success although the requested final path was absent and the final existed only under the retained renamed parent. Exchanging `RAW.parent` just before the first raw child read likewise returned all four payloads although the requested `RAW` path was absent; the loader's final `os.stat(RAW.name,dir_fd=parent)` observes the old pinned parent, not its replacement in the namespace.
    impact — descriptor-relative leaf checks alone do not establish custody under the frozen named namespace or reject ancestor exchange as required by the canonical plan lines 138-144 and remediation M1/M2.
    fix — preserve the complete directory identity chain and revalidate each named parent-child binding from the trusted anchor at acceptance/one-way boundaries; fail closed on detached/exchanged ancestry. Apply the same primitive to creation, publishers, inventories, raw reads, and opaque private reads. Add immediate-parent and higher-ancestor exchange regressions, not only raw-leaf exchange tests.
  - [high] scripts/prepare_msae_independent_norspan_v1.py:2388-2395,2459-2462,2532-2534 — successful acquisition validation has exact top-level keys but incomplete literal/type/nested semantic reconstruction.
    reasoning — independent actual synthetic acquisition mutations were accepted by `validate_control_chain`: `source_content_printed=True`, `model_operations=False`, `sparse_checkout=1`, `scratch={"cleanup_verified":"yes"}`, and `local_object_type_counts={"mystery":-999}`. With `source_content_printed=True`, actual isolated `cmd_prepare` followed by `cmd_verify` emitted `verified_ready_not_scored`. The flag is not validated, operation counters use Python equality with zero, and sparse/cleanup fields use truthiness; successful scratch/object-count/file-entry schemas are not fully reconstructed.
    impact — the successful terminal verifier can accept contradictory privacy evidence and malformed custody metadata despite the shared strict-schema acceptance. Hardened rejection/phase schemas do not close the success path.
    fix — apply canonical literal/type comparisons to every successful acquisition flag/counter; validate exact nested scratch/file/object-count schemas, allowed kinds and nonnegative integer counts, and reconstruct all derivable consistency relations. Reject contradictory `source_content_printed` evidence. Add successful acquisition and full prepare/verify mutations for each field and nested schema, including Boolean-as-integer negatives.
  - [high] scripts/acquire_msae_independent_norspan_v1.py:49-52 — selector initialization occurs after child creation but outside cleanup protection.
    reasoning — injecting `DefaultSelector` initialization failure raised immediately while the owned synthetic Python leader remained running, despite `timeout_seconds=0.1`. The `finally` block is never entered. I explicitly killed/reaped this test child and closed its pipes afterward; no process was left running by the review.
    impact — an ordinary supervisor startup/resource failure after durable network entry can leave an unsupervised subprocess executing source access without the frozen deadline or promised termination/reaping.
    fix — initialize selector resources before spawning, or protect every post-spawn initialization step with cleanup that tolerates a missing selector. Add startup/registration/read exceptions and interruption tests asserting owned group termination, leader reap, closed pipes, preserved initiation evidence, and no repeat operation.

REVISIONS
  - None beyond the blocking custody/schema/supervision findings.

NITS
  - None.

CHECKS RUN
  - `bin/check-plan` → PASS against the governing amended source-free task plan.
  - Bounded public diff inspection using `/tmp/norspan-remediation-before/` snapshots, not Git's omission-prone untracked diff; inspected canonical plan, preserved BLOCK reviews, current config, acquisition/preparation/replay boundaries and synthetic test isolation.
  - `PYTHONDONTWRITEBYTECODE=1 pytest -q -p no:cacheprovider tests/test_prepare_msae_independent_norspan_v1.py` → 172 passed, 2 skipped in 54.33s. The skips are the documented inapplicable single-file mkdir/child-directory-fsync boundaries, not unexplained exclusions or protocol approval.
  - Independent isolated temp counterexamples → all five successful-acquisition mutations accepted; printed-source contradiction survived actual prepare/verify; public/raw immediate-parent exchanges accepted; original-temp-inode substitutions accepted by all three publishers; selector initialization failure left owned leader running until reviewer cleanup.
  - Exact reviewed inputs, checked before/after and unchanged: builder 2b5172a01274c4d42847e430e2c540a1a239493a528ff6b39d1cabc004f43cb3; runner dc8a1ed3525deec3356f785f636ae5f19be6175eb6681be02ec96e97b3ff7db3; config 645fc34db2a770113c6c8ef531e9317f3209e4d6f1d31c18b55daecad6151420; tests d5c0cd95590bab850e8fd3525dfb661b25ff3ae597c8c271f553daf8fb33c0d8; current source-free log 9e1642d8241981789c0a1ce3203afcfeaa9117d9fcfc4ae065322940c0d7f373.
  - Preserved original review digests unchanged: implementation BLOCK 3cc1f07361beb0a3f4d7523df1636de3bb4de5871f02c2b1950412be137bf6f6; bounded-follow-up BLOCK ee0df6e680764eaaf09b37d59b8428dfe9a2615256afe75ea2e0e19d35464f5d.
  - Compile, red-before-repair history, whitespace checks and isolated predecessor subset were read as retained log evidence, not independently rerun. The current log distinguishes 162 passing isolated predecessor cases and five withheld cases from the historical 167-case result.
  - Scope audit → no real history census/build-history/build-authority/acquisition/prepare/verify operation, real candidate/provenance namespace access, protected Atlas or predecessor content read/hash, source network, GPU inventory, model/tokenizer, scoring/training/K2/branch, tmux, final-test opening, or implementation/config/test/log edit. Actual control commands used only new isolated synthetic roots, fake Git and synthetic host observations. Only this separate review report was persisted.

CONTRACT COVERAGE
  - M1 UTF-8 boundary and retained private interruption evidence → met locally — incremental probe and bounded full-adapter negatives pass; private exception cleanup no longer erases partial objects.
  - M1 opaque private comparison → met locally in the exercised non-exchange scope — bounded chunk SHA/byte/newline counts replace actual private full-byte materialization/semantic parsing; same-size content substitution is detected by replay.
  - M1/M2 owned publisher/ancestor custody → unmet — original-inode and parent-exchange counterexamples above survive the new descriptor helpers.
  - M2 complete fresh accessible-history reenumeration/exact additions and control metadata → partial — new complete enumeration catches ignored additions, empty directories and edits, and actual mode/nlink plus canonical duplicate/nonfinite-safe phase/authority controls are tested. Ancestor binding and complete successful acquisition semantics remain unsafe; static recensus improvement is not custody closure.
  - M3 pre-start recovery, initiated no-retry, ordinary timeout/output supervision and strict initial object/worktree matching → partial — both network/raw pre-start fixture resumes and durable-start no-repeat checks pass; ordinary bounded drains, timeout and pipe-holding descendant termination pass. Startup cleanup counterexample remains.
  - M4 actual synthetic success/rejection and supported first scientific failure reconstruction → partial — actual pipeline success, license rejection, fresh rejection capability/history checks, retained-failure mutation and scientific operational-unresolved behavior pass. Successful acquisition contradictory evidence is still declared ready.
  - M5 exact current logs, independent review and honest distinction from launch → met as evidence/status discipline — exact frozen hashes and preserved prior BLOCK reviews; current transcript explicitly keeps BLOCK/NOT READY and records interim NFS anomalies and predecessor exclusions. Technical remediation completion is not achieved because the above code blockers remain.
  - Full combined marker/publication/exchange/kill/cardinality fault matrix → partial and explicitly unclosed — bounded tests at applicable publisher boundaries are real, but the current transcript lines 88-92 correctly withholds exhaustive closure.
  - Scientific source, role cutpoints, overlap/support thresholds, branch predicates and predecessor dispositions → unchanged within inspected remediation diff. No actual scientific gate or independent-source result is required for this source-free review, and none is claimed.
  - Real acquisition/model/GPU/scoring/training/launch authority → not granted — whole launch remains NOT READY. This separate report neither supersedes the canonical primary BLOCK nor broadens its finite authority/alias table.

UNKNOWNS
  - The interim NFS publication_metadata anomalies recorded in the current log remain unexplained. My current 172-case pass does not resolve their cause or authorize retry/relaxation of attempted objects.
  - The full combined fault matrix, source-readiness/contained prescore implementation and reviews, G3/C1/C2/M5-M10 and release remain unclosed or outside this review.
  - Actual candidate tree/content/license/groups/support/overlap and real namespace absence remain intentionally uninspected. Synthetic positive fixtures are software evidence, not scientific source evidence.
  - Current real process/training eligibility was not queried; point-in-time synthetic snapshots do not prove absence of ephemeral activity.
  - A future replacement authority must be prospectively resolved without treating a new review filename as implicit canonical authority or silently changing the frozen plan. Preserve this BLOCK report and obtain a separately frozen independent successor review after any repair.
