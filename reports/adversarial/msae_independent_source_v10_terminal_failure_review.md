VERDICT: SHIP
ONE-LINE: V10 is a valid terminal B1 scientific-gate rejection at cross-role overlap—not a model result or independent replication—and must not be retried under the V10 protocol.

BLOCKERS        (must fix before proceeding; empty if none)
  - None.

REVISIONS       (should fix; not blocking)
  - None.

NITS            (optional, cap at 5)
  - Interpret `blocking_collision_count=100` as the retained fail-fast witness count, not an exhaustive population estimate: `_role_overlap` returns as soon as 100 witnesses are collected (`scripts/prepare_msae_independent_source_v10.py:1584-1597`). Any positive count is sufficient for the frozen gate.

CHECKS RUN
  - `sha256sum`/canonical-JSON checks on the complete retained path -> acquisition entry `4dcb100124a8acf0eca9c394de1a9ea0b5c3f97a5229bd0bf1ca52ab73aa76af`; acquisition `eab784620840cefc532dc72681c36640443ddeaf1f08eee6b4cffd545d3b2a1d`; scientific entry `e3c17556cce3913ca5bb16b00acb0e75b90ab4a030bd26e711304cdf155c1573`; source family `a5b53c4a48d0c2751c681958e40233cd43fe081e338335d6fd7654d48d0f6056`; candidate pedigree `dbbb8b278dd0fac93d317a3158f240217002f805c559fdf0b3a158a84ecd70e3`; dedup `d2e7a8fcec07b1defb7fe928caeae23f46a2a036afadbead81aed0e5bd90e403`; cross-role overlap `ce81b37b2357fa2aafe39cc8df81763c1d7b73798d993f2912e633b2cd4dad9a`; rejection `16def212308c69501a7f589be8304f16dbfaaea66b69211dd03f1c6a25b49f05`. Every retained public JSON is canonical.
  - Public acquisition validator reconstruction with raw-byte validation deliberately disabled -> exact entry/success schemas, authority/baseline/review/config/runner bindings, supplied review hash, commit `c434778d9511be5c35a6a11531f0107a960fb5d6`, four-file tree, command argv/order, eleven zero-exit command records, stdout/stderr byte counts and digests, Git tree/blob evidence, empty clone/checkout output, and zero model/GPU/training counts all pass the frozen checks at `scripts/acquire_msae_independent_source_v10.py:434-676`.
  - Raw custody lstat-only reconstruction -> exact namespace shape `data/msae_independent_source_v10/raw/<commit>/` with only `LICENSE.txt`, train, dev, and test; parents are mode `0700`, immutable commit directory mode `0555`, every raw final is a regular mode-`0444`, nlink-1 file at the acquisition-reported size, and no private directory exists. No raw bytes or hashes were read.
  - Acquisition scratch metadata check -> reported `/tmp/msae-v10-acquire-s_il5epv` scratch root is absent, consistent with successful cleanup before publication. Acquisition report status is success rather than a retained acquisition rejection.
  - Scientific-entry reconstruction -> status `entered_before_first_v10_raw_open`; `raw_file_open_count_before_entry=0`; exact plan/review/builder/carryover/baseline/authority/authority-review/acquisition hashes; frozen history digest `8070a7cdd536a0dbd6104ff1dfb279ef01fb0acf98e969227625324155227564`; frozen training digest `2e655873dee4824cda59ca18bfbbde3b3f62e86f454860fbf2d1af4235779fc5`; eligible pre-entry process snapshot with 40 records and zero forbidden identities; all model/GPU/training/scoring/K2/Stage-C fields false or zero.
  - Exact prefix/cardinality reconstruction using the reviewed implementation's public-only/lstat-only helpers -> classified B1, not B0/B2/case-C; scientific prefix is exactly the first seven frozen artifacts in order: document census, source manifest, license, source family, candidate pedigree, dedup, cross-role overlap. Entry plus this contiguous prefix plus rejection are the only scientific finals; every named temp is absent; history manifest/overlap, support, role/split manifests, post-process snapshot, no-training gate, and seal are absent. This matches `docs/plan-msae-independent-source-v10.md:282-313` and `scripts/prepare_msae_independent_source_v10.py:3604-3705`.
  - Source-family public evidence -> schema/status `eligible`, disposition `project_source_use_unseen_before_v10`, zero content occurrences, zero pathname occurrences, zero whole-file digest matches, no source text published, and exact frozen-history digest binding.
  - Candidate-pedigree public evidence -> status `eligible`, registry SHA `3166d09141374506d7d200fa8d630a66e4a790f5a3f2e81778ed522d4a327863`, eight candidate identifier hashes, zero blocking identifiers, and per-file hash-only summaries for all four candidate files.
  - Pre-overlap scientific prefix consistency -> document census is eligible with 85 explicit `newdoc` markers, zero cross-partition group-ID collisions, and no document/speaker inference; source manifest reports 4,315/504/1,219 train/dev/test sentences; license is eligible CC-BY-SA-4.0 from `LICENSE.txt` only; dedup retains 4,290 discovery, 503 calibration, and 1,217 test sentences after within-role drops and reports zero cross-partition normalized groups.
  - Cross-role evidence schema/semantics -> status `ineligible`; exactly 100 unique retained witness records, all between distinct roles; 98 use frozen reason `contained_short` and two use `exact`; no unsupported reason or same-role witness appears. The builder publishes this artifact then raises `cross_role_overlap` before the history-overlap/support/payload stages (`scripts/prepare_msae_independent_source_v10.py:3024-3037`).
  - B1 rejection reconstruction -> exact rejection schema/status `rejected_after_scientific_entry`, failure `cross_role_overlap`, raw-open count 35, scientific-entry hash `e3c175...`, exact baseline `65795d91db32e415b58e3ef7af68d81e96f682a97c44f1c580a094ba23825127`, authority `3178d973f57a5e93a29123d364e973484d5ea7ff408bbda2da0ab9620c4ecbf1`, authority review `078f00feeb45003206728ecd1a6edc3212a712181c980488f6ae9eaf0f02f468`, and `next_action=new_reviewed_protocol_only`. Its self-final scientific inventory and B1 post-baseline inventory reconstruct exactly from current public files and lstat metadata.
  - History/training evidence -> embedded history recensus is `eligible`, with expected=observed baseline entries digest `f462340e56c277cf88e5ed9f62cad80943645bfda531ccbbb2b14279ff2d93c8`, expected=observed history digest `8070a7cd...`, no added/removed/changed paths, and zero quarantine reads. Embedded training recensus is `eligible`, expected=observed digest `2e655873...`, and exactly 2,116 entries. Rejection binds these objects byte-for-byte to the scientific entry.
  - Payload/authorization closure -> `data/msae_independent_source_v10/private/` and payload are absent; rejection and lstat agree. Seal, no-training gate, history-overlap, support, role, and split outputs are absent. `source_content_reported=false`; model/GPU/training operations are zero; model scoring, K2/branch training, and Stage C are false.
  - Current informational process recensus using the frozen snapshot routine -> `eligible`, 32 visible records, zero forbidden identities. No GPU query was issued.
  - Terminal verifier evidence -> the operator-provided invocation completed exit 0 with no output. Independently, the current public/lstat-only audit reproduced its acquisition public schema, scientific-entry historical reconstruction, exact B1 prefix/cardinality, rejection self-inventory, current B1 post-baseline inventory, payload absence, recensus evidence schemas/bindings, and fresh eligible process condition. The verifier's B1 path is intentionally zero-work and does not authorize raw reconstruction (`scripts/prepare_msae_independent_source_v10.py:3671-3705`).
  - Public custody -> all 16 V10 provenance finals are regular mode-`0644`, nlink 1 files; Git HEAD remains the baseline-bound `7e1a6fc7a26a870a4edcf29c96b7cdf3cdbd7faa`.
  - Prohibited-operation audit -> no V8/V9/V10 raw bytes or hashes, private content, or quarantine content were opened/read/parsed/printed; no network, acquisition, model, tokenizer, GPU, scoring, training, K2, or branch command was run.

CONTRACT COVERAGE
  - One-shot acquisition authenticity -> met at the provenance/control-plane level — exact authority gate, immutable commit, exact four-file tree/blob evidence, command transcripts/digests, raw lstat custody, and scratch cleanup reconstruct. Source bytes were deliberately not reread in this provenance-only review.
  - Support/history checks before scoring -> met as a fail-closed sequence — source family and pedigree passed, but cross-role overlap failed at frozen step 8, before full history overlap, support, role publication, split, payload, or any model scoring (`docs/plan-msae-independent-source-v10.md:282-313`).
  - Source-family and pedigree gates -> met — both retained canonical eligible evidence with zero source-family hits and zero blocking identifiers; these narrow project-history claims do not establish researcher/model-pretraining independence.
  - Cross-role gate correctness -> met — the retained artifact contains 100 schema-valid cross-role blocking witnesses under the frozen exact/short-containment rules, so the required first-failure code is `cross_role_overlap` regardless of later unexecuted gates.
  - Exact B1 artifact prefix/cardinality -> met — contiguous seven-artifact prefix, entry, and rejection only; no gap, later final, temp, payload, gate, or seal.
  - B1 historical reconstruction -> met — entry-time and rejection-time post-baseline inventories reconstruct state-relatively, including generated-directory child sets and raw lstat records, without reading raw/private bytes.
  - History/training/process containment -> met — retained recensuses report exact no-drift digests, 2,116 unchanged training entries, zero quarantine reads, eligible pre-entry process evidence, and current process evidence remains eligible.
  - Capability closure -> met — no model/tokenizer/GPU/scoring/training/K2/branch/Stage-C operation or authorization occurred.
  - No-retry closure -> met — V10 has consumed both one-shot entries and reached canonical B1. `next_action=new_reviewed_protocol_only`; retrying acquisition or preparation inside V10 would violate the reviewed protocol.
  - Claim boundary -> met — this is a control-plane/scientific data-gate rejection. It is not model evidence, not a score, not a replication result, and specifically not independent replication. Exposed-source V3 remains calibration only.

UNKNOWNS
  - Under the mandatory provenance-only boundary, this review did not independently recompute raw SHA/Git-blob identities or the normalized-text relationships behind the 100 collision witnesses. It verified the acquisition's public cryptographic command evidence, raw lstat custody, the builder's retained public summaries, and the fail-closed B1 state; the raw-derived semantics remain bounded to the one-shot builder evidence.
  - The reported zero-output, exit-0 terminal-verifier invocation has no separate durable verification transcript in the reviewed V10 public directory. Its current B1 conditions were independently reconstructed here without rerunning the prohibited full verifier or reading protected bytes.
  - History/training recensus byte scans were not rerun because they would cross the explicit private-content prohibition. Their canonical eligible evidence is bound identically in the entry and rejection, and the immediately preceding authority review independently rehashed every non-private baseline file and all 2,116 training-root entries.
