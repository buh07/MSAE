VERDICT: BLOCK
ONE-LINE: Two retained drift cells still accept unrelated failures and evaluate an unguarded namespace observation before primary-preserving finalization.

BLOCKERS
  - [high] tests/test_msae_norspan_typed_scientific_wave3.py:265-273 — The initiating drift prepare still accepts any GateFailure, RuntimeBlocked or PairFailure and never requires all four raw consumers to complete.
    reasoning — Mutation firing is asserted, but an unrelated PairFailure after ordinal 1 or 3 can satisfy the broad initiating catch before history science runs. Fresh raw-started refusal, preserved mutated bytes, and one loader invocation can all still pass in that case. The actual ordinary history-content predicate raises GateFailure('history_drift:' + rel) at scripts/prepare_msae_independent_norspan_v1.py:2009-2012; its SHA reader compares the same original metadata tuple, and the controlled mutation is same-length content. The successor now types fresh refusals, but not this independently required initiating refusal.
    impact — Both drift cells can remain green without proving the claimed actual history-content drift predicate fired. This leaves the prior causal-refusal obligation unresolved.
    fix — Require n.GateFailure with an anchored, escaped exact history_drift:synthetic-history.txt reason; capture the actual reason as causal evidence. Assert tracker.calls == 1 and len(tracker.completed) == 4 before crediting history science, alongside exactly one intended mutation firing and the retained post-fresh changed bytes. Preserve RED/BLOCK for another refusal reason or unexpected terminal success.

  - [medium] tests/test_msae_norspan_typed_scientific_wave3.py:274-277 — physical_seal=(n.PROV / 'seal.json').exists() is evaluated before observe() receives its arguments, outside the primary-preserving guard.
    reasoning — Python evaluates keyword values before invoking observe. If that namespace observation raises OSError while the drift body is unwinding an assertion or initiating failure, observe's try/except at lines 28-36 never runs. The namespace error replaces the original primary, and the intended drift facts are not emitted. Other callbacks now correctly execute their namespace facts under observe, making this a concrete remaining call-site escape, not a criticism of the strengthened helper.
    impact — The prior failure-safe finalization/nonreplacement obligation remains unmet for the two drift cells, despite the outer cell's stronger FD and serialization handling.
    fix — Move this observation into the guarded callback, for example a lambda returning tracker.facts plus the seal observation and other finite fields. Attach secondary observation errors to the active primary, retain the original failure and still attempt FD/property finalization; fail the cell if observation fails without a primary.

REVISIONS
  - [medium] tests/test_msae_norspan_typed_scientific_wave3.py:165-200,389-400 — No direct assertion preserves the outside producer (path, hash, sequence) tuple after EACH fresh attempt.
    reasoning — Exact physical snapshot equality is now enforced at line 188, but the in-memory current_catalog_pin remains only an emitted property at line 199. Silent store rebasing without physical mutation is not equivalent to evidence retention, and the test's direct pin equality at line 389 occurs only before fresh attempts.
    impact — The declared unchanged outside producer pin/no-adoption claim is less discriminating than the now-strong physical preservation oracle.
    fix — Capture before_pin before the fresh loop and assert pin(f) == before_pin immediately after every attempted explicit load/recover/prepare boundary, including cardinality refusal. Emit both exact tuples, without rebasing them from observations.

  - [medium] tests/test_msae_norspan_typed_scientific_wave3.py:243,275-277,372,400 — Early RED finalization still lacks current producer pin and synthetic-command count when an assertion fails before fresh_refusal.
    reasoning — tracker.facts emits loader/read/private-attempt counters but not current pin or len(f.calls); hook.facts emits the predecessor but not the post-fault current tuple. The fresh helper now safely emits these facts in its own finally, but that does not run if an earlier firing/pin/shape assertion fails.
    impact — The retained prior review's RED pin/transport-property revision is only partially addressed; a failed cell can lack required all-cell causal pin/count evidence.
    fix — Add safely observed current_catalog_pin and synthetic_git_commands to per-cell final callbacks once the fixture exists, independent of reaching fresh_refusal. Record explicit unavailable facts if setup failed rather than inventing observations. Keep actual phase/error trace visible.

NITS
  - None.

CHECKS RUN
  - nl -ba and sha256sum of the current test source — reviewed exact SHA-256 3ee85bb92ce89bf2fbffac33dbcb1158634f0f7900594569d3e0faa1d8eb8a5f, 400 source lines; no imports, collection or execution.
  - Static source cross-check against the approved v3 and expressly allowed public files only: preparer protocol inventory/started-state refusal/history_collisions/SHA helper; runtime _Directory.validate; Fixture.fresh. Typed directory_cardinality, started_without_terminal_no_retry, raw_started_or_partial_unresolved and raw_started_unresolved oracles align with these public source paths.
  - Finite permitted read of reports/verification/msae_norspan_typed_scientific_wave3_active_supersession.json — old candidate hash 7a515e5cfcd585dc3cc0989c10540067e19daf7c65183983d843845d6f931c22 remains UNQUALIFIED_ARCHIVAL_REGARDLESS_OF_PASS_COUNT; current hash matches the source reviewed here and is recorded unlaunched/PENDING. whole_groups_open=9, whole_rows_open=80, whole_matrix_qualified=false, production_launch_authorized=false.
  - No old active-run completion/result read, no completion wait, no new test run or harness invocation. Persisted only this distinct mode-0644 report, leaving all prior plans/reviews/source/archive artifacts unchanged.

CONTRACT COVERAGE
  - Prior blocker: exact post-fresh physical retention → met statically — retained_snapshot binds named synthetic control/data/catalog trees to original identity/type/mode/nlink and regular-file size/SHA maps (lines 71-95). It includes ordinary-history identity/content SHA, asserts exact map equality after EACH fresh boundary (line 188), and drift rereads changed bytes before/after attempts (lines 271,273). Runtime evidence is pending.
  - Prior blocker: explicit load versus execute typed fresh phase → met statically — lines 175-187 separate expected catalog-load directory_cardinality from mandatory successful noncatalog loading and exact command type/reason. Fixture.fresh is the explicit producer-pinned CatalogStore.load path (fixture lines 154-157); no discover-latest path is added. The initiating drift refusal part remains unmet as above.
  - Prior blocker: private entry/retry-attempt detection → met statically — pass-through counters are at real publish_private_roles entry and real create_owned_pair entry before underlying calls (lines 104-127). Private tests bind one producer entry and exact prior/target role creation attempts (lines 368-369); fresh attempts assert no counter changes (lines 190-191). These can detect an early existing-stage refusal after an attempted retry.
  - Prior blocker: primary-safe observations, FD errors, serialization → substantially met statically — observe executes callbacks under a primary-preserving BaseException guard (lines 26-36); cell records failed FD observation, attempts JUnit emission, preserves active primary and fails unestablished cleanup/observations (lines 45-68). Private/terminal namespace callbacks are guarded lambdas (lines 372,400). Drift's evaluated keyword remains a blocker.
  - Corrected pair-method forwarding, targeted original stage/catalog faults and reachable predecessor capture → retained statically — lines 113-118,298-335. Exactly one first-write/link/catalog fault uses original identities; stage-write capture is before actual os.write, link/catalog capture before promotion; terminal test requires unchanged initiating tuple (line 389).
  - Actual scientific predicates/real loader/publication and finite cell count → retained prospectively — pass-through producer instrumentation, no scientific outcome replacement; parameter sets remain 2 positives + 8 raw + 2 drift + 16 private + 12 terminal = 40. No runtime firing/result attested here.
  - Raw/source no-retry and private preservation → stronger statically — exact refusal reasons, per-operation physical equality and unchanged real counters are now discriminating; direct post-operation tuple equality remains a revision.
  - All-cell causal JUnit/pin/count evidence on RED → partial — stronger failure-safe finalization, but drift guard escape and early-failure pin/command evidence still need correction.
  - Production subjects/guard → no production edit made by this reviewer; allowed public runtime still has unconditional production BLOCK. This review does not audit global immutable manifests or the staged367-file checkpoint.
  - Whole nine groups/80 whole rows → OPEN; whole production technical approval → BLOCK. No scientific eligibility, real source authority, scoring/training/model/launch authority is inferred from this static source or any old forty-cell pass count.

UNKNOWNS
  - BLOCK concerns synthetic test-harness acceptance/qualification, not a demonstrated production defect. No test of this exact successor was launched, imported, collected or executed; runtime firing, FD outcomes, properties, retention and exception chains remain unverified.
  - The initial eight-cell pre-loader TypeError RED and the old forty-cell candidate remain preserved and unqualified. Their outcomes cannot qualify the current changed hash.
  - The approved prospective v3 SHIP remains a plan-only decision; prior implementation BLOCK remains unchanged. Any further source fix requires its own exact binding/source/runtime evidence review, not adaptation of the old run.
  - Read scope: current tests/test_msae_norspan_typed_scientific_wave3.py, approved plan/prior review findings and only tests/msae_norspan_controller_fixture.py, tests/test_msae_norspan_typed_history_wave2.py, tests/test_msae_norspan_jpc_full_recovery.py, scripts/prepare_msae_independent_norspan_v1.py, scripts/msae_norspan_jpc_controller.py, scripts/msae_norspan_jpc_runtime.py, scripts/msae_jumbo_pair_commit.py plus the expressly permitted supersession JSON. No actual fixture trees or protected/private/blind/history/key/state objects were inspected.
  - No tests/imports, process census, network/acquisition, namespace/cgroup operations, model operation, real source/payload/scoring/training work, staging/commit operation or production modification occurred. Whole owner approval/canonical binding/FINAL receipt remain pending; V10 remains terminal and later gates/AMALGUMv3 receive no authority here.
