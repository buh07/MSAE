VERDICT: BLOCK
ONE-LINE: Fresh Jumbo probes establish destructive scratch-custody defects; neither historical causation nor a cleanup amendment is approved.

BLOCKERS
  - [critical] scripts/acquire_msae_independent_norspan_v1.py:195-210,213-221 — scratch-root identity is mistaken for authority to delete every child.
    evidence — `remove_tree_fd` enumerates all current names, recursively removes directories, and unlinks every non-directory without a creation/ownership ledger or a foreign-entry rejection. `cleanup_scratch` checks only the scratch root's device/inode before invoking it. The exact new synthetic `foreign_extra` case returned without error after native `unlinkat(..., "foreign", 0) = 0`; both tracked and foreign entries and the scratch directory were absent. Independent trace lines 1059-1064 and report case `foreign_extra` establish this directly.
    reasoning — pinning a directory identifies a namespace; it does not prove that entries added by another actor belong to this invocation. This counterexample needs no race or substituted scratch root. Descriptor-relative operations and O_NOFOLLOW do not establish child ownership.
    impact — cleanup can destroy a foreign obstruction while reporting successful cleanup, contrary to the no-destructive-foreign-evidence contract. Ordinary closed-reader cleanup passes cannot close this defect.
    fix — stop real use of this cleanup. The proposed simpler future direction is preservation of every initiated scratch subject with no recursive deletion or retry, but it requires a separately reviewed retention/placement/size/quota/authority/schema amendment and production regressions. That direction is not implemented or approved here; do not delete any retained old subject.

  - [high] scripts/acquire_msae_independent_norspan_v1.py:197-209 — the checked non-directory name can designate a different inode at unlink.
    evidence — the new probe's lines 40-53 execute a controlled substitution at the `os.unlink` boundary after production's stat: rename the original `tracked` to `retained-original`, exclusively create/close a different-inode `tracked` with fixed foreign bytes, then invoke the real unlink. Independently checked original device 65/inode 36273498 differs from replacement device 65/inode 36277268. Native `renameat` occurs at trace line 1118, the replacement is fsynced/closed, and `unlinkat(..., "tracked", 0) = 0` occurs at line 1123. The retained original preserves its original inode and exact 31-byte dummy contents; the foreign replacement name is absent.
    reasoning — production uses the stat only for directory classification. Its non-directory branch does not validate ownership or bind unlink to the checked inode. The deterministic in-process hook models a legal namespace transition between two path-based syscalls; it does not demonstrate the frequency of concurrent external interference.
    impact — scratch cleanup deletes a substituted foreign object even though it later raises `OSError` errno 39. Rejection is not preservation. The intentionally retained original makes this synthetic `rmdir` fail with ENOTEMPTY after foreign deletion; that errno cannot imply rollback or custody safety.
    fix — do not treat another pathname check, directory pinning, holder closure, or an ENOTEMPTY retry as a repair. Prospective no-deletion scratch preservation removes this destructive boundary, subject to separate contract/authority review and exact-byte implementation tests. No such repair is approved or performed in this review.

  - [high, static finding] scripts/acquire_msae_independent_norspan_v1.py:375-395 — cleanup runs on all acquisition exceptions and on success, erasing initiated partial scratch evidence.
    evidence — the `except BaseException` branch calls the same recursive deletion before deciding whether the error is a supported command rejection. This covers interrupts, parse/internal faults and source-custody failures as well as command failures. Successful cleanup discards scratch contents and leaves only root identity plus `cleanup_verified=True`. If cleanup itself raises, execution does not reach the original-error classification or rejection publication; both scratch descriptors are closed by `finally`. The success branch also deletes scratch and records `cleanup_verified=True`.
    reasoning — the root identity and removal flag are not a retained inventory or before-image of initiated partial work. Cleanup is unconditional with respect to failure class and child ownership. This conclusion follows from the inspected control flow; no actual acquisition, fake acquisition pipeline, rejection publication, prepare or verify was executed here.
    impact — this cannot meet a preserve-all-initiated-scratch/foreign/partial-evidence contract. Removing these calls alone is also insufficient because terminal/rejection scratch schemas currently assert verified removal and their consumers/authority must agree with any prospective retention semantics.
    fix — keep whole launch BLOCK. Separately review any preservation amendment across runner placement, exception/success behavior, retained scratch metadata, strict terminal/rejection consumers, capacity/size/quota/retention responsibilities and exact authority bindings. Preserve original errors, never reacquire initiated source work, and exercise combined custody/interrupt/IO faults in the later source-free qualification. No production changes are authorized by this finding.

REVISIONS
  - None for acceptance of this exact bounded latent-defect finding. The unchanged production cleanup remains blocked as above.

NITS
  - None.

CHECKS RUN
  - Public source inspection: acquisition `remove_tree_fd`, `cleanup_scratch`, exception/success branches and probe lines 1-83; import-time public protocol definitions and main guard inspected. No source/census/provenance/prepare/verify entrypoint invoked.
  - Exact frozen source hashes before and after the independent run: production acquisition SHA256 `7aa98231181fd19061726a68b004079a0c13310d0fcfa7f7938f45ef61ae2f4c`, mode 0755; new synthetic probe SHA256 `e89f5fc23b33e137ec14a210758b4f13fa3417b81528a98012f2b466ad57e82f`, mode 0755. Production and probe bytes were not edited.
  - Original supplied probe report read and hash-checked: `/jumbo/lisp/f004ndc/tmp/lisplab1/norspan-jumbo-scratch-custody-NyqBll/subjects/report.json`, SHA256 `f0915a9839b4b5c56da6e34429e1903b970381c2c4a6417f35269676a9a63d37`. Its sibling syscall trace confirms successful native foreign/unsubstituted unlinks, controlled substitution, successful replacement unlink and subsequent ENOTEMPTY. No original subject cleanup/retry occurred.
  - Independent reproduction command, project working directory: `umask 077; R=$(mktemp -d /jumbo/lisp/f004ndc/tmp/lisplab1/norspan-jumbo-custody-independent-XXXXXX); TMPDIR="$R" PYTHONDONTWRITEBYTECODE=1 strace -f -yy -s 200 -o "$R/syscalls.log" -e trace=%file,process,network,fsync,fdatasync,fstat,close,getdents64 python3 -B scripts/diagnose_msae_scratch_custody_v1.py --root "$R/subjects" > "$R/stdout.json" 2> "$R/stderr.log"` -> exit 0; both reported destructive synthetic foreign cleanup. Exit0 means the diagnostic completed, not that cleanup custody passed.
  - Fresh independent retained run root: `/jumbo/lisp/f004ndc/tmp/lisplab1/norspan-jumbo-custody-independent-mFcg2R`. Two newly created subjects only; no older root reused or removed. Report/stdout SHA256 both `c2643a2a65d91b33b3ea28342e208f9128fe729b5a3c1bfde8f325bf7795fa24`; trace SHA256 `0d5ec2a954394654d41924d4bbfac095c78026a01c745f316686357b4748759d`.
  - Independent fixed-byte assertions -> PASS: each case's outside-subject owned before-image is exactly `fixed owned synthetic evidence` plus newline (31 bytes); each foreign before-image is exactly `fixed injected foreign evidence` plus newline (32 bytes), SHA256 `4f3fdfb5e08d9313141747d4220bc3bfed4380feeaa82bc239ede6fec070eb5f`; foreign-extra scratch absent; replacement inode differs; retained-original inode and bytes unchanged; report equals stdout; second error errno 39. No destructive assertion used real source or protected bytes.
  - Independent syscall-trace scan -> no network syscall and no actual NORSPAN protocol-data/provenance or prohibited Atlas/V8/V9/V10 content path event in the traced invocation. File/process/network event tracing is bounded evidence, not a general absence-of-activity certificate.
  - Contract inspected: prospective Jumbo qualification v3 constraints lines 11-21 and Q1/Q4/definition-of-done lines 106-110,123-128,142-151 require retained evidence, no old cleanup/retries, no false reliability/historical-causation claim, and whole BLOCK where fault/evidence conditions remain unmet. Existing primary implementation review remains BLOCK and is not superseded.

CONTRACT COVERAGE
  - Reliable latent implementation defect under the stated no-destructive-foreign-evidence contract -> met as a finding — two simple legal namespace states fail against exact unchanged production cleanup, independently rerun with native syscall evidence. This is not production correctness approval.
  - Preserve original and injected synthetic evidence outside the subject -> met for these completed runs — exact owned/foreign before-images retained; substituted original also retained inside scratch; neither original nor independent run was retried or cleaned. Synthetic backups prevent unrecoverable diagnostic byte loss, not the defect's occurrence inside scratch.
  - Jumbo-only new artifacts, source-free/public-code-only bounds -> met for this review — newly written synthetic evidence and this report are under Jumbo; bytecode disabled; no real acquisition/census/provenance/source/prepare/verify/model/GPU/tmux/scoring/training or protected-content operations were performed.
  - No arbitrary-child/substituted-object deletion in production -> unmet — the first two blockers and native unlink results.
  - Preserve initiated partial acquisition scratch -> unmet by static production control flow — unconditional exception and success deletion; actual acquisition behavior not exercised in this review.
  - Historical scratch ENOTEMPTY/publication_metadata root-cause attribution -> intentionally unclosed — this ENOTEMPTY is caused by the probe's newly retained original. No historical subject inspected or cleaned, no old cause inferred, and no `.nfs` mechanism demonstrated by this particular probe.
  - Prospective no-deletion scratch amendment -> unapproved/unimplemented — schema, lineage/authority, size/quota, retention duties and full fault matrix must be separately reviewed. Existing `cleanup_verified=True` assertions cannot be silently reinterpreted.
  - Whole production readiness/launch authority -> unmet/carried BLOCK — this separate report is not the canonical primary/authority-review alias, supplies no source/recovery/scoring authorization, and changes no older verdict or retained report.

UNKNOWNS
  - This is a controlled single-client synthetic counterexample, not an estimate of real attack/race probability or proof against all child/root/directory substitutions, combined faults, server reboot or multi-client behavior.
  - The probe is robust enough for this exact reviewed invocation: fresh exclusive root, fixed dummy subjects, no acquisition call, closed injected writer, captured different inode, restored monkeypatch, no cleanup retry, retained exact before-images and corroborating native syscalls. It is not a general-purpose production-safe deletion tool. Its `/jumbo` root guard (lines 73-75) is lexical, not an ancestor/symlink containment proof; observed device 65 and decoded trace targets support this controlled Jumbo run only.
  - Probe `foreign_name_preserved`/`result` (lines 63-67) check pathname existence, not ownership or byte equality. Acceptance here relies additionally on successful real unlink syscalls, confirmed injection/different inode, directory snapshots and independent fixed-byte assertions; pathname flags or exit 0 alone are insufficient evidence for future runs.
  - Probe inventory is a sequence of lstat observations, not an atomic snapshot, and report/before-image creation is not a crash-durability certification. No broader filesystem reliability, quota/reservation, backup or retention guarantee follows from these completed runs.
  - The complete production phase/publication/custody/kill/combined-fault matrix, exact prospective authority reconciliation and all scientific/readiness/scoring/training gates remain outside this bounded review and unclosed. No public or protected source bytes were read to assess them.
