VERDICT: BLOCK
ONE-LINE: Five synthetic counterexamples expose cleanup misclassification, lost evidence, incomplete setup budgets, permissive result framing and a namespace-capable fixture race.

BLOCKERS
  - [high] scripts/msae_norspan_containment_preflight.py:331-335,349-352,412-415 — A control-FD close fault after an observed denied pids.max write replaces the arm-level primary and is labeled UNAVAILABLE_CGROUP_SETUP instead of cleanup BLOCK.
    reasoning — The unguarded finally close raises a secondary; classification consults only whether the last logged operation failed and the replacement exception's errno. It does not distinguish operation denial from cleanup failure. Counterexample A performs a synthetic EACCES write failure, releases the local control FD, then raises EACCES from close: status is UNAVAILABLE_CGROUP_SETUP, error names only the close fault, and cleanup_errors is absent. The original write error remains in the operation list but primary/secondary roles are not preserved. All owned FDs are physically restored, so the launch gate permits D2 on this status.
    impact — Accepted plan:87-89 requires every cleanup fault to preserve the primary and BLOCK, never be downgraded to an unavailable prerequisite or authorize another arm.
    fix — Capture control/read-FD close errors separately without replacing the initiating exception; preserve structured primary/secondary fields; make any cleanup error BLOCK regardless of physical FD restoration. Add the denial-plus-after-release-close-failure regression and assert D2 is not launched.
  - [medium] scripts/msae_norspan_containment_preflight.py:349-354 — D1's exception serialization drops __notes__ produced by parked(), including target kill/reap failures.
    reasoning — parked():275 appends secondary cleanup errors to the primary's notes, but cgroup_preflight records only repr(exc). Counterexample B raises a parked primary with the note 'cleanup incomplete: synthetic target reap failure'; D1 is BLOCKED_UNKNOWN, but neither notes nor the secondary text appear anywhere in its returned evidence. D2's catch at line416 already preserves notes, showing the asymmetric path.
    impact — Accepted plan:87-89 and definition-of-done:146-148 require retained secondary cleanup results, not merely a failed status.
    fix — Serialize D1 primary and all original notes/secondary cleanup details before adding leaf cleanup errors; test that the final persisted arm evidence contains both the initiating failure and target-reap fault.
  - [high] scripts/msae_norspan_containment_preflight.py:294-343,188-218 — D1's setup30s budget starts only inside parked(), after all delegated-directory/leaf/control setup has completed; cgroup_preflight has no deadline checks.
    reasoning — Counterexample C advances an artificial monotonic clock by100s during each successful local leaf allocation. Both subsequent control writes and the parked callback still occur; it is reached at clock210 from initial10, after200s of setup. This is not an OS-uninterruptible wait claim: the operations have returned successfully, and the harness still initiates later work without checking the expired budget.
    impact — Accepted plan:81 requires a separate fixed monotonic setup30s deadline for the arm; partial progress must not refresh it or allow new initiated operations after expiry.
    fix — Establish one D1 deadline at arm entry, check it between setup operations and before child creation/GO, and pass the remaining original setup budget into parked() rather than start a new30s phase. Add an artificial-clock regression asserting no further write or child launch after expiry, while retaining partial nodes without retry/deletion.
  - [medium] scripts/msae_norspan_containment_preflight.py:105-109,249-257 — The active parked-result boundary accepts the two-field simplified classification fixture schema and omits required own before/after observations.
    reasoning — Counterexample D launches an actual inert no-fork synthetic child that returns canonical RESULT with only errno0/returncode0. parked('userns') accepts it as AVAILABLE_PREREQUISITE_ONLY, go_sent=True and owned_child_reaped=True, although no own namespace observation is present. The fixture exception inside classify() leaks into active protocol acceptance.
    impact — Accepted plan:118 and its identity/result evidence requirement demand own namespace handles before/after; malformed/missing protocol evidence cannot be promoted to the same result as the reviewed handler.
    fix — Keep simplified pure classification fixtures separate from strict active result validation. At parked() require exactly returncode/errno/before/after with exact integer and observation-string types; update GOOD fixtures to that real schema and test missing/wrong-type observations rejection.
  - [high] tests/test_msae_norspan_containment_preflight.py:244,250-256; scripts/msae_norspan_containment_preflight.py:240-245,159-165 — The allegedly no-operation killed-owner fixture can send GO to a real userns-capable child if its receiving/killing supervisor is delayed beyond the finite3s callback sleep.
    reasoning — send() transports the original target pidfd, sleeps3s and returns normally. parked() then unconditionally writes GO. Parked phase uses _argv=None, selecting the actual userns handler; the early wrapper also ultimately calls that handler. Counterexample E uses the exact SCM_RIGHTS/send/sleep/return callback with an inert child and intentionally does not schedule the receiver until after return: GO_sent_before_receiver_processed_transfer=True, and the synthetic child completes. No real namespace operation was used to demonstrate the race. The existing fixture's 'no_GO':True is printed as a constant, not recorded from a gate.
    impact — Accepted plan:129-137 requires fixture target exit with no diagnostic operation; the review scope prohibits real cgroup/userns calls during this synthetic suite. Sleep is not an ownership barrier and cannot make a namespace-capable target safe under scheduling/transport/kill failures.
    fix — Make fixture targets deliberately incapable of cgroup/userns operations while exercising the actual parent-death logic. After pidfd transfer, the owner callback must never return normally toward GO: use a controlled barrier whose EOF/error always fails closed, plus original-parent-pidfd/PDEATHSIG ownership for the fixture coordinator. Supervise the outer fixture through its original pidfd, without numeric-kill timeout fallback. Add delayed-receiver and transport/kill/bootstrap failure tests proving no GO and no real diagnostic capability on every path.

REVISIONS
  - None beyond the blocking corrections.

NITS
  - None.

CHECKS RUN
  - Read only the named public implementation, named synthetic suite, runtime contract and accepted wave1-v2 plan, plus explicit public verification root pointers. No legacy test collection/import occurred.
  - Exact reviewed SHA256s: script ebd548ddc5855896491b19d1dd5ad478083694034de64337b120cfb3b3c3b325; tests 9c95568f00724d0b39c41aa0da8dd5c260f81b7ad339a4db525d93095d362f6f; runtime contract 05b07ab2bcf781d19499fe3f59632bc7941648d9821cc137f0489a8e8c0a7b0c; plan 6efcf2485abb00ee962e31ec258e840cf7d95e5959aecaca6fc574784453e946.
  - Initial independent pytest launcher used -S and failed with 'No module named pytest'; no tests collected. Retained /jumbo/lisp/f004ndc/tmp/lisplab1/norspan-preflight-independent-review-USLw3PM4.
  - PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 /usr/bin/python3.12 -I -B -m pytest -c /dev/null --noconftest -p no:cacheprovider --basetemp <fresh-root>/base --junitxml <fresh-root>/results.xml tests/test_msae_norspan_containment_preflight.py -q → 41 passed in3.50s, exit0. Retained /jumbo/lisp/f004ndc/tmp/lisplab1/norspan-preflight-independent-suite-AxSdfCAp. No other test file was collected.
  - Bounded standalone public-module/local-regular-file counterexamples A-D → all four fired, exit0, empty stderr and restored own FD baselines. Source/results retained under /jumbo/lisp/f004ndc/tmp/lisplab1/norspan-preflight-adversarial-probes-1J64BaGo/probes.py and results.json.
  - Bounded inert-child/anonymous-SCM_RIGHTS counterexample E → finite3s callback returned and GO was sent before receiver processing; exit0, empty stderr and restored own FD baseline. Retained /jumbo/lisp/f004ndc/tmp/lisplab1/norspan-preflight-fixture-barrier-probe-5eCMADkq/probe.py and results.json.
  - Exact source/test/contract/plan copies and their hashes retained under counterexample A-D root/reviewed-subjects; no existing source, test, guard, protocol, TODO, V10 or older report was edited.
  - No --run invocation, real cgroup/namespace syscall, source/raw/private/history/blind/key/payload/model access, foreign process scan, network, GPU or training. All direct counterexamples used owned inert children/local regular files/anonymous fixture sockets only. Only this distinct mode0644 review was added to the repository.

CONTRACT COVERAGE
  - Once-only no-fork active child argv/environment/pass_fds and original parent/target pidfd signal custody → met in inspected happy-path structure, with direct-unreaped/default-SIGCHLD/single-thread checks and PDEATHSIG before READY.
  - Startup missing-target-handle cooperative EOF and original-child wait → partial; existing startup fixtures pass, but noncooperative EOF/secondary retained-failure evidence requires stronger end-to-end D1 coverage.
  - Exact migration/pids control bytes and two retained leaves → met in constants and happy-path local fixture; control cleanup classification and full-arm setup budget are unmet.
  - Primary/secondary cleanup preservation and unavailable-versus-BLOCK sequencing → unmet; counterexamples A/B fire.
  - Setup30s/command10s/cleanup5s and bounded output/framing → partial; parked budgets/output-cap tests pass, but D1 setup is outside its deadline and required result observations are optional.
  - Actual FD reuse without re-closing a substituted selector handle → met for the named observed close-after-release regression; broader control-close faults remain unmet.
  - Killed-owner original-pidfd transport and adoption/reap happy paths → exercised by the41-pass suite; no-operation safety under delayed receiver → unmet, counterexample E.
  - Finite ownmaps/imported public source/cache/preload runtime closure → implemented as a bounded public manifest; exact accepted launch subjects and final frozen evidence remain pending, not a containment guarantee.
  - Production guard/whole9/80/scientific boundaries → inspected preflight results explicitly remain production_launch_authorized=False, containment_qualified=False, whole_matrix_closed=False. No production/science authority or Stage1 closure is granted by this review.

UNKNOWNS
  - Real kernel migration/userns permissions remain untested; active prerequisites are not authorized until the scoped implementation blockers are corrected and independently reviewed.
  - Exact final runtime/argv/evidence binding must still freeze all accepted subjects; this review does not accept a generic caller-written SHIP field as scientific or production authority.
  - Arbitrary same-UID adversaries, uninterruptible/scheduling bounds, full descendant/fork/escape/bubblewrap/network/protected-content/server qualification and whole80-row closure remain out of scope.
