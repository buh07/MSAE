# Pre-K2 Suite v6 Reports

This directory stores summarized outputs copied or linked from a v6 run root:

- `aggregate_results.tsv`
- `per_layer_aggregate.tsv`
- `ci_metrics.tsv`
- `threshold_sensitivity.tsv`
- `pre_k2_decision.json`
- `pre_k2_decision.md`

Populate this folder after run completion and final review, then commit with tag `pre-k2-suite-v6`.

