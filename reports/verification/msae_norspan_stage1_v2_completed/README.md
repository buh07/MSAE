# Completed NORSPAN stage1 v2 public checkpoint
2026-09-17. Engineering reproduction only; **not whole qualification or science authority**.

Both exact31-suite runs completed1172PASS,0failure/error/skip,exit0. Original logs,
commands and deterministic gzip copies of original JUnit are retained here. Their
uncompressed SHA256/bytes and compressed artifact identities are bound in
`../msae_norspan_stage1_v2_completed_results.json`. The corresponding independent
review is `../../adversarial/msae_norspan_stage1_v2_completed_evidence_review.md`.
Historical PENDING records, older archives and all earlier BLOCKs remain unchanged.

`public-checkpoint.tar.gz` contains exactly78declared regular members:76named public
subjects plus MANIFEST.json/REPRODUCE.py. No fixture trees/disposable keys, actual
history/source/raw/private/blind/payload/credentials/Git/state are included. Legacy
preparer test is a public source subject only; NEVER import/collect/execute it.

To reproduce the finite engineering checkpoint, inspect archive members against the
manifest, extract ONLY these declared regular members into a NEW Jumbo directory,
apply exact manifest modes (Git records only the executable bit), then run the extracted
`/usr/bin/python3.12 -B REPRODUCE.py /jumbo/lisp/f004ndc/tmp/lisplab1/NEW-UNUSED-OUTPUT`
from the extracted root. This script takes ONE positional output path, not `--help`;
inspect its public source before running. Its
runner validates inputs and invokes pytest; it does NOT itself safely extract an
archive. Extractor source inspection is not adversarial negative-execution approval.
Do not overwrite an existing root or discover/adopt a state. Keep output outside
frozen inputs and retain failed roots. Fresh synthetic Fixture asserts the current
approved Jumbo scratch prefix; this is not a generic non-Jumbo portability promise.

The original commands preserve exact host paths as provenance, not authorization to
reuse an old initiated output/basetemp. New runs must use new synthetic directories.
Do not infer new added-suite coverage from these exact76/1172results.

Client-side artifact fsync returned. Server reboot/failover/backup/retention/quota
and historical durability remain UNKNOWN. All9groups/80whole rows remain OPEN;
production's first unconditional guard remains enabled. No production source/payload/
scoring/K2/branch/G3/C2 operation is authorized by these files.
